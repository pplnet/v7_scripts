creardialogo
entero1:'Codigo';;;;;;Query("Select count(*) from "~Dbo()~".NUMERADORES where Codigo="~FStr(dialogo.entero1, 0, 0)~" ") > 0;'El Numerador es INEXISTENTE !!!';NO;
string1:'Tipo';;;;NO;;;;;;Query("Select Tipo from "~Dbo()~".NUMERADORES where Codigo="~FStr(dialogo.entero1, 0, 0)~" ");;;
entero2:'Numero';;;;NO;;;;;;Query("Select Numero from "~Dbo()~".NUMERADORES where Codigo="~FStr(dialogo.entero1, 0, 0)~" ");;;
findialogo

if (not ok)
Cancelar()
endif

Act(a1, Query("Select Codigo from "~Dbo()~".VEHICULOS where Alias1 Like '"~FStr(dialogo.entero1, 0, 0)~"%' "))

if (Vacio(Val(a1)))
Sqladd("DELETE "~Dbo()~".NUMERADORES Where  Codigo="~FStr(dialogo.entero1, 0, 0)~" ")
Sqlnew()
Sqlexec()
else
Messagebox('El Tipo de Numerador esta asignado al Vehiculo '~Val(a1))
endif



