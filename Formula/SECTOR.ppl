IIF(Incluido(Var('INSTALACIO'), Var('INSCONTSTD')), IIF(#ESCP ,'NFI',BuscarCampo('CATEGORIACLIENTES','Sector','Codigo', fbn('CategDepositante'))),IIF(Eqstr(Var('INSTALACIO'), 'MLASA'),IIF(Incluido(fbn('CategDepositante'),'OTR|CLI'),'NFI', 'FIN')))

*IF incluido(Var('INSTALACIO'), Var('INSCONTSTD'))
*    BuscarCampo('CATEGORIACLIENTES','Tipo','Codigo', fbn('CategDepositante'))
*ELSE
*    IF Eqstr(Var('INSTALACIO'), 'MLASA')
*        IF Incluido(fbn('CategDepositante'),'OTR|CLI'))
*            'NFI'
*        ELSE
*            'FIN'
*        ENDIF
*    ENDIF
*ENDIF
