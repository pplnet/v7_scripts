CrearFont(1,"Arial,8,")
CrearFont(2,"Arial,8,B")

SetFilaActual(4)
ACN(w1,FAC)
ACN(A1, FAC)

ACT(a:FAC, 'Tipos de Transacciones')
ACT(a:FAC+2,'Codigo')
ACT(b:FAC+2,'Nombre')
ACT(c:FAC+2,'Especie Hab.')
ACT(d:FAC+2,'Cod.2')
AgregarMarco(a:FAC+2..e:FAC+2,2,0,Exe('COLOR'))
SetFont(a:FAC..d:FAC+2,2)

SetFilaActual(FAC+2)

SQL.ADD("Select * From "~DBO~".TIPOSTRANSACCION2")
recorrer SQL
  SCN(w2,1)
  ACT(a:FAC, FBN('Codigo'))
  ACT(b:FAC, FBN('Nombre'))
  if NoVacio(FBN('Especie'))
    ACT(c:FAC, FBN('Especie'))
  else
    ACT(c:FAC, 'TODAS')
  endif
*  ACT(d:FAC, FBN('Codigo2'))
proximo
AgregarMarco(a:Val(w1)+3..e:FAC+1,1)
SetFont(a:Val(w1)+3..d:FAC,1)

SetFilaActual(FAC+2)
ACT(a:FAC, 'Cantidad de Registros: '~FStr(Val(w2),1,0))
SetFont(a:FAC, 2)

ACO(2,25)
ACO(4,6)

Impresion(1,1)
SetFilaActual(FAC+2)
ACT(a:FAC, 'FIN DEL LISTADO')
SetFont(a:FAC, 2)
AgregarBMP(a1,Exe('LOGO'),60)
LimitesVision(d:FAC)
RangoTitulo(a:1..d:FAC,Val(w1)+2)
