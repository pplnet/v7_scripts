CrearFont(1,"Arial,8,")
CrearFont(2,"Arial,8,B")
CrearFont(3,"Arial,10,B")
AgregarBMP(a2,Exe('LOGO'))
SetFilaActual(4)
ACN(w1,FAC)
AgregarBMP(a1,Exe('LOGO'),60)
ACT(b:FAC-1,'Aranceles Clientes')
ACT(a:FAC,'Fecha : ')
ACT(b:fac,var('FECHASYS'))
ACT(a:FAC+2,'Arancel')
ACT(b:FAC+2,'Cliente')
ACT(c:FAC+2,'Porcentaje')
ACT(d:FAC+2,'Minimo')
ACT(e:FAC+2,'Fijo')
ACT(f:FAC+2,'BasePorcentaje')
ACT(g:FAC+2,'Maximo')

AlinearCeldas(b:fac-1..b:fac-1,'C')
SetFont(B:FAC-1..B:FAC-1,3)
AgregarMarco(a:FAC+2..g:FAC+2,2,0,Exe('COLOR'))
SetFont(a:FAC+2..g:FAC+2,2)

SetFilaActual(FAC+2)

SQL.ADD("Select * From "~DBO~".ARANCELESCLIENTE")

recorrer SQL
  SCN(w2,1)
  ACT(a:FAC, FBN['Arancel'])
  ACT(b:FAC, FBN['Cliente'])
  ACN(c:FAC, FBN['Porcentaje'])
  ACN(d:FAC, FBN['Minimo'])
  ACN(e:FAC, FBN['Fijo'])
  ACN(f:FAC, FBN['BasePorcentaje'])
  ACN(g:FAC, FBN['Maximo'])
proximo

AgregarMarco(a:Val(w1)+3..g:FAC,1)
SetFont(a:Val(w1)+3..g:FAC,1)

ACO(2,12)
ACO(3,12)
ACO(4,12)
ACO(5,12)
ACO(6,12)
ACO(8,1)

Impresion(1,1)
SetFilaActual(FAC+2)
ACT(a:FAC, 'FIN DEL LISTADO')
SetFont(a:FAC, 2)
LimitesVision(h:FAC)
RangoTitulo(a:1..j:FAC,Val(w1)+2)