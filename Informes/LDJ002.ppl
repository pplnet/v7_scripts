CrearFont(1,"Arial,8,")
CrearFont(2,"Arial,8,B")
CrearFont(3,"Arial,10,B")

SetFilaActual(4)
ACT(a:FAC, 'Listado Parametros DDJJ')
SetFont(a:fac..b:fac,3)

SetFilaActual(fac+2)
ACT(a:fac,'Codigo')
ACT(b:fac,'Descripcion')
ACT(c:fac,'Mercado')
ACT(d:fac,'Tipo Cli')
ACT(e:fac,'Tipo Esp')
ACT(f:fac,'Archivo')
SetFont(a:fac..f:fac,2)
AgregarMarco(a:FAC..f:FAC,1,0,Exe('COLOR6'))
ACN(w:1, FAC)

SQL.ADD(" Select * ")
SQL.ADD("From "~DBO~".PARAMETROSDDJJ ")
SQL.ADD(" order by Codigo ")  
Recorrer SQL
  SCN(x:1, 1)
  ACT(a:FAC, fbn('Codigo'))
  ACT(b:FAC, fbn('Descripcion'))
  ACT(c:FAC, fbn('Mercado'))
  ACT(d:FAC, fbn('TipoCli'))
  ACT(e:FAC, fbn('TipoEsp'))
  ACT(f:FAC, fbn('Archivo'))
  SetFont(a:fac..f:fac,1)
proximo

if Val(x:1)=0
  SetFilaActual(fac+2)
  ACT(a:FAC, 'Registros en la tabla')
  SetFont(a:fac..a:fac,2)
endif

ACO(1,10)
ACO(2,20)
ACO(3,6)
ACO(4,6)
ACO(5,6)
ACO(6,40)

Impresion(1,2)
SetFilaActual(fac+2)
ACT(a:fac,'FIN DEL INFORME')
SetFont(a:fac,2)
LimitesVision(f:FAC)
RangoTitulo(a:1..u:fac+1, Val(w:1))

