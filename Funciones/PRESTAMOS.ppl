scn(f:200,-9)
scn(g:200,-9)

SQL.ADD("Select O.Vehiculo1, O.Especie, O.FechaVto Fecha, O.NrOperacion, O.TipoOp, ")
SQL.ADD("Sum(C.Cantidad1)/1000 Cant from "~DBO~".OPERACIONES  O, "~DBO~".CUOTAS C ")
SQL.ADD("where O.NrOperacion = C.NrOperacion ")
SQL.ADD(" and  O.FechaOp  <= '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and  C.Fecha2   >  '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and  O.TipoOp In ('MMPCLM') ")
SQL.ADD(" and  O.Vehiculo1 In ("~Pipe2Comillas(Dialogo.Lista1)~") ")
SQL.ADD(" and  O.Especie Not In ("~val(en1)~") ")
SQL.ADD(" group by O.NrOperacion, O.Vehiculo1, O.Especie, O.FechaVto, O.TipoOp ")
SQL.ADD(" Union All ")
SQL.ADD("Select O.Vehiculo1, O.Especie, O.FechaVto Fecha, O.NrOperacion, O.TipoOp, ")
SQL.ADD("Cantidad/1000 Cant from "~DBO~".OPERACIONES  O ")
SQL.ADD("where O.FechaOp  <= '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and  O.FechaVto >  '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and  O.TipoOp In ('MMPCL','MMPLME') ")
SQL.ADD(" and  O.Vehiculo1 In ("~Pipe2Comillas(Dialogo.Lista1)~") ")
SQL.ADD(" and  O.Especie Not In ("~val(en1)~") ")
SQL.ADD("order by 1, 2 ")

act(u:200,' ')
Recorrer SQL
  if Not EqStr(val(u:200),fbn['Vehiculo1'])
     Recorrer&i 3, val(b:200)
        if EqStr(fbn['Vehiculo1'],val(&i:2))
           acn(e:200,&i)
           Break
        endif
     Proximo
     act(u:200,fbn['Vehiculo1'])
  endif

  If Not EqStr(val(aa:200),fbn['Especie'])
     *- Cotizacion
     ACN(zz2, CotizacionC(fbn['Especie'],,,,Dialogo.Fecha1,1,SI,Val(me1))) 
     ACT(aa:200, fbn['Especie'] ) 
  Endif

 
  if EqStr(fbn['Especie'],'ARP')
     scn(val(e:200):val(f:200),fbn['Cant'])
     scn(val(e:200):val(i:200),fbn['Cant'])
     SetFont(val(e:200):val(f:200)..val(e:200):val(f:200),3)
    else
     scn(val(e:200):val(g:200),fbn['Cant']*Val(zz2))
     scn(val(e:200):val(l:200),fbn['Cant']*Val(zz2))
     SetFont(val(e:200):val(g:200)..val(e:200):val(g:200),3)
  endif
  if fbn['Fecha'] <= (Dialogo.Fecha1+365)
     scn(val(e:200):val(v:200),fbn['Cant']*Val(zz2))
     scn(b:val(v:200),fbn['Cant']*Val(zz2))
     SetFont(val(e:200):val(v:200)..val(e:200):val(v:200),3)
    else
     if Not Incluido(fbn['TipoOp'],'MMPCLM')
        scn(val(e:200):val(w:200),fbn['Cant']*Val(zz2))
        scn(b:val(w:200),fbn['Cant']*Val(zz2))
        SetFont(val(e:200):val(w:200)..val(e:200):val(w:200),3)
     else
        SQL.ADD("Select Sum(Cantidad1)/1000 Parcial from "~DBO~".CUOTAS ")
        SQL.ADD("where  NrOperacion = '"~fbn['NrOperacion']~"' ")
        SQL.ADD("  and  Fecha2 <= '"~IdxDate(Dialogo.Fecha1+365)~"' ")
        SQL.ADD("  and  Fecha2 >  '"~IdxDate(Dialogo.Fecha1)~"' ")

        acn(y:200,0)
        Recorrer SQL
          acn(y:200,fbn['Parcial'])
        Proximo

        scn(val(e:200):val(v:200),val(y:200)*Val(zz2))
        scn(b:val(v:200),val(y:200)*Val(zz2))
        SetFont(val(e:200):val(v:200)..val(e:200):val(v:200),3)
        scn(val(e:200):val(w:200),(fbn['Cant']-val(y:200))*Val(zz2))
        scn(b:val(w:200),(fbn['Cant']-val(y:200))*Val(zz2))
        SetFont(val(e:200):val(w:200)..val(e:200):val(w:200),3)
     endif
  endif
proximo

scn(g:200,1)
scn(f:200,1)

SQL.ADD("Select O.Vehiculo1, O.Especie, O.FechaVto Fecha, O.NrOperacion, O.TipoOp, ")
SQL.ADD("Sum(C.Cantidad1)/1000 Cant from "~DBO~".OPERACIONES  O, "~DBO~".CUOTAS C ")
SQL.ADD("where O.NrOperacion = C.NrOperacion ")
SQL.ADD(" and  O.FechaOp  <= '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and  C.Fecha2   >  '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and  O.TipoOp In ('MMPOSM') ")
SQL.ADD(" and  O.Vehiculo1 In ("~Pipe2Comillas(Dialogo.Lista1)~") ")
SQL.ADD(" and  O.Especie Not In ("~val(en1)~") ")
SQL.ADD(" group by O.NrOperacion, O.Vehiculo1, O.Especie, O.FechaVto, O.TipoOp ")
SQL.ADD(" Union All ")
SQL.ADD("Select O.Vehiculo1, O.Especie, O.FechaVto Fecha, O.NrOperacion, O.TipoOp, ")
SQL.ADD("Cantidad/1000 Cant from "~DBO~".OPERACIONES  O ")
SQL.ADD("where O.FechaOp  <= '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and  O.FechaVto >  '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and  O.TipoOp In ('MMPCOS','MMPSC','MMSCMV') ")
SQL.ADD(" and  O.Vehiculo1 In ("~Pipe2Comillas(Dialogo.Lista1)~") ")
SQL.ADD(" and  O.Especie Not In ("~val(en1)~") ")
SQL.ADD("order by 1, 2 ")

act(u:200,' ')
Recorrer SQL
  if Not EqStr(val(u:200),fbn['Vehiculo1'])
     Recorrer&i 3, val(b:200)
        if EqStr(fbn['Vehiculo1'],val(&i:2))
           acn(e:200,&i)
           Break
        endif
     Proximo
     act(u:200,fbn['Vehiculo1'])
  endif

  If Not EqStr(val(aa:200),fbn['Especie'])
     *- Cotizacion
     ACN(zz2, CotizacionC(fbn['Especie'],,,,Dialogo.Fecha1,1,SI,Val(me1))) 
     ACT(aa:200, fbn['Especie'] ) 
  Endif

  if EqStr(fbn['Especie'],'ARP')
     scn(val(e:200):val(f:200),fbn['Cant'])
     scn(val(e:200):val(i:200),fbn['Cant'])
     SetFont(val(e:200):val(f:200)..val(e:200):val(f:200),3)
    else
     scn(val(e:200):val(g:200),fbn['Cant']*Val(zz2))
     scn(val(e:200):val(l:200),fbn['Cant']*Val(zz2))
     SetFont(val(e:200):val(g:200)..val(e:200):val(g:200),3)
  endif
  if fbn['Fecha'] <= (Dialogo.Fecha1+365)
     scn(val(e:200):val(v:200),fbn['Cant']*Val(zz2))
     scn(b:val(v:200),fbn['Cant']*Val(zz2))
     SetFont(val(e:200):val(v:200)..val(e:200):val(v:200),3)
    else
     if Not Incluido(fbn['TipoOp'],'MMPOSM')
        scn(val(e:200):val(w:200),fbn['Cant']*Val(zz2))
        scn(b:val(w:200),fbn['Cant']*Val(zz2))
        SetFont(val(e:200):val(w:200)..val(e:200):val(w:200),3)
       else
        SQL.ADD("Select Sum(Cantidad1)/1000 Parcial from "~DBO~".CUOTAS ")
        SQL.ADD("where  NrOperacion = '"~fbn['NrOperacion']~"' ")
        SQL.ADD("  and  Fecha2 <= '"~IdxDate(Dialogo.Fecha1+365)~"' ")
        SQL.ADD("  and  Fecha2 >  '"~IdxDate(Dialogo.Fecha1)~"' ")

        acn(y:200,0)
        Recorrer SQL
          acn(y:200,fbn['Parcial'])
        Proximo

        scn(val(e:200):val(v:200),val(y:200)*Val(zz2))
        scn(b:val(v:200),val(y:200)*Val(zz2))
        SetFont(val(e:200):val(v:200)..val(e:200):val(v:200),3)
        scn(val(e:200):val(w:200),(fbn['Cant']-val(y:200))*Val(zz2))
        scn(b:val(w:200),(fbn['Cant']-val(y:200))*Val(zz2))
        SetFont(val(e:200):val(w:200)..val(e:200):val(w:200),3)
     endif
  endif
proximo

scn(f:200,1)
scn(g:200,1)

SQL.ADD("Select O.Vehiculo1, O.Especie, O.FechaVto Fecha, O.NrOperacion, O.TipoOp, ")
SQL.ADD("Sum(C.Cantidad1)/1000 Cant from "~DBO~".OPERACIONES  O, "~DBO~".CUOTAS C ")
SQL.ADD("where O.NrOperacion = C.NrOperacion ")
SQL.ADD(" and  O.FechaOp  <= '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and  C.Fecha2   >  '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and  O.TipoOp In ('MMPEM') ")
SQL.ADD(" and  O.Vehiculo1 In ("~Pipe2Comillas(Dialogo.Lista1)~") ")
SQL.ADD(" and  O.Especie Not In ("~val(en1)~") ")
SQL.ADD(" group by O.NrOperacion, O.Vehiculo1, O.Especie, O.FechaVto, O.TipoOp ")
SQL.ADD(" Union All ")
SQL.ADD("Select O.Vehiculo1, O.Especie, O.FechaVto Fecha, O.NrOperacion, O.TipoOp, ")
SQL.ADD("Cantidad/1000 Cant from "~DBO~".OPERACIONES  O ")
SQL.ADD("where O.FechaOp  <= '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and  O.FechaVto >  '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and  O.TipoOp In ('MMPE') ")
SQL.ADD(" and  O.Vehiculo1 In ("~Pipe2Comillas(Dialogo.Lista1)~") ")
SQL.ADD(" and  O.Especie Not In ("~val(en1)~") ")
SQL.ADD("order by 1, 2 ")

act(u:200,' ')
Recorrer SQL
  if Not EqStr(val(u:200),fbn['Vehiculo1'])
     Recorrer&i 3, val(b:200)
        if EqStr(fbn['Vehiculo1'],val(&i:2))
           acn(e:200,&i)
           Break
        endif
     Proximo
     act(u:200,fbn['Vehiculo1'])
  endif

  If Not EqStr(val(aa:200),fbn['Especie'])
     *- Cotizacion
     ACN(zz2, CotizacionC(fbn['Especie'],,,,Dialogo.Fecha1,1,SI,Val(me1))) 
     ACT(aa:200, fbn['Especie'] ) 
  Endif


  if EqStr(fbn['Especie'],'ARP')
     scn(val(e:200):val(f:200),fbn['Cant'])
     scn(val(e:200):val(i:200),fbn['Cant'])
     SetFont(val(e:200):val(f:200)..val(e:200):val(f:200),3)
    else
     scn(val(e:200):val(g:200),fbn['Cant']*Val(zz2))
     scn(val(e:200):val(l:200),fbn['Cant']*Val(zz2))
     SetFont(val(e:200):val(g:200)..val(e:200):val(g:200),3)
  endif
  if fbn['Fecha'] <= (Dialogo.Fecha1+365)
     scn(val(e:200):val(v:200),fbn['Cant']*Val(zz2))
     scn(b:val(v:200),fbn['Cant']*Val(zz2))
     SetFont(val(e:200):val(v:200)..val(e:200):val(v:200),3)
    else
     if Not Incluido(fbn['TipoOp'],'MMPEM')
        scn(val(e:200):val(w:200),fbn['Cant']*Val(zz2))
        scn(b:val(w:200),fbn['Cant']*Val(zz2))
        SetFont(val(e:200):val(w:200)..val(e:200):val(w:200),3)
       else
        SQL.ADD("Select Sum(Cantidad1)/1000 Parcial from "~DBO~".CUOTAS ")
        SQL.ADD("where  NrOperacion = '"~fbn['NrOperacion']~"' ")
        SQL.ADD("  and  Fecha2 <= '"~IdxDate(Dialogo.Fecha1+365)~"' ")
        SQL.ADD("  and  Fecha2 >  '"~IdxDate(Dialogo.Fecha1)~"' ")

        acn(y:200,0)
        Recorrer SQL
          acn(y:200,fbn['Parcial'])
        Proximo

        scn(val(e:200):val(v:200),val(y:200)*Val(zz2))
        scn(b:val(v:200),val(y:200)*Val(zz2))
        SetFont(val(e:200):val(v:200)..val(e:200):val(v:200),3)
        scn(val(e:200):val(w:200),(fbn['Cant']-val(y:200))*Val(zz2))
        scn(b:val(w:200),(fbn['Cant']-val(y:200))*Val(zz2))
        SetFont(val(e:200):val(w:200)..val(e:200):val(w:200),3)
     endif
  endif
proximo

scn(f:200,1)
scn(g:200,1)

SQL.ADD("Select O.Vehiculo1, O.Especie, O.FechaVto Fecha, O.NrOperacion, O.TipoOp, ")
SQL.ADD("Sum(C.Cantidad1)/1000 Cant from "~DBO~".OPERACIONES  O, "~DBO~".CUOTAS C ")
SQL.ADD("where O.NrOperacion = C.NrOperacion ")
SQL.ADD(" and  O.FechaOp  <= '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and  C.Fecha2   >  '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and  O.TipoOp In ('MMPIM') ")
SQL.ADD(" and  O.Vehiculo1 In ("~Pipe2Comillas(Dialogo.Lista1)~") ")
SQL.ADD(" and  O.Especie Not In ("~val(en1)~") ")
SQL.ADD(" group by O.NrOperacion, O.Vehiculo1, O.Especie, O.FechaVto, O.TipoOp ")
SQL.ADD(" Union All ")
SQL.ADD("Select O.Vehiculo1, O.Especie, O.FechaVto Fecha, O.NrOperacion, O.TipoOp, ")
SQL.ADD("Cantidad/1000 Cant from "~DBO~".OPERACIONES  O ")
SQL.ADD("where O.FechaOp  <= '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and  O.FechaVto >  '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and  O.TipoOp In ('MMPI') ")
SQL.ADD(" and  O.Vehiculo1 In ("~Pipe2Comillas(Dialogo.Lista1)~") ")
SQL.ADD(" and  O.Especie Not In ("~val(en1)~") ")
SQL.ADD("order by 1, 2 ")

act(u:200,' ')
Recorrer SQL
  if Not EqStr(val(u:200),fbn['Vehiculo1'])
     Recorrer&i 3, val(b:200)
        if EqStr(fbn['Vehiculo1'],val(&i:2))
           acn(e:200,&i)
           Break
        endif
     Proximo
     act(u:200,fbn['Vehiculo1'])
  endif

  If Not EqStr(val(aa:200),fbn['Especie'])
     *- Cotizacion
     ACN(zz2, CotizacionC(fbn['Especie'],,,,Dialogo.Fecha1,1,SI,Val(me1))) 
     ACT(aa:200, fbn['Especie'] ) 
  Endif

  if EqStr(fbn['Especie'],'ARP')
     scn(val(e:200):val(f:200),fbn['Cant'])
     scn(val(e:200):val(i:200),fbn['Cant'])
     SetFont(val(e:200):val(f:200)..val(e:200):val(f:200),3)
  else
     scn(val(e:200):val(g:200),fbn['Cant']*Val(zz2))
     scn(val(e:200):val(l:200),fbn['Cant']*Val(zz2))
     SetFont(val(e:200):val(g:200)..val(e:200):val(g:200),3)
  endif

  if fbn['Fecha'] <= (Dialogo.Fecha1+365)
     scn(val(e:200):val(v:200),fbn['Cant']*Val(zz2))
     scn(b:val(v:200),fbn['Cant']*Val(zz2))
     SetFont(val(e:200):val(v:200)..val(e:200):val(v:200),3)
    else
     if Not Incluido(fbn['TipoOp'],'MMPIM')
        scn(val(e:200):val(w:200),fbn['Cant']*Val(zz2))
        scn(b:val(w:200),fbn['Cant']*Val(zz2))
        SetFont(val(e:200):val(w:200)..val(e:200):val(w:200),3)
       else
        SQL.ADD("Select Sum(Cantidad1)/1000 Parcial from "~DBO~".CUOTAS ")
        SQL.ADD("where  NrOperacion = '"~fbn['NrOperacion']~"' ")
        SQL.ADD("  and  Fecha2 <= '"~IdxDate(Dialogo.Fecha1+365)~"' ")
        SQL.ADD("  and  Fecha2 >  '"~IdxDate(Dialogo.Fecha1)~"' ")

        acn(y:200,0)
        Recorrer SQL
          acn(y:200,fbn['Parcial'])
        Proximo

        scn(val(e:200):val(v:200),val(y:200)*Val(zz2))
        scn(b:val(v:200),val(y:200)*Val(zz2))
        SetFont(val(e:200):val(v:200)..val(e:200):val(v:200),3)
        scn(val(e:200):val(w:200),(fbn['Cant']-val(y:200))*Val(zz2))
        scn(b:val(w:200),(fbn['Cant']-val(y:200))*Val(zz2))
        SetFont(val(e:200):val(w:200)..val(e:200):val(w:200),3)
     endif
  endif
proximo

scn(f:200,1)
scn(g:200,1)

SQL.ADD("Select O.Vehiculo1, O.Especie, O.FechaVto Fecha, O.NrOperacion, O.TipoOp, ")
SQL.ADD("Sum(C.Cantidad4)/1000 Cant from "~DBO~".OPERACIONES  O, "~DBO~".CUOTAS C ")
SQL.ADD("where O.NrOperacion = C.NrOperacion ")
SQL.ADD(" and  O.FechaOp  <= '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and  C.Fecha2   >  '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and  O.TipoOp In ('MMONM') ")
SQL.ADD(" and  O.Vehiculo1 In ("~Pipe2Comillas(Dialogo.Lista1)~") ")
SQL.ADD(" and  O.Especie Not In ("~val(en1)~") ")
SQL.ADD(" group by O.NrOperacion, O.Vehiculo1, O.Especie, O.FechaVto, O.TipoOp ")
SQL.ADD(" Union All ")
SQL.ADD("Select O.Vehiculo1, O.Especie, O.FechaVto Fecha, O.NrOperacion, O.TipoOp, ")
SQL.ADD("TotalBrutoCli3/1000 Cant from "~DBO~".OPERACIONES  O ")
SQL.ADD("where O.FechaOp  <= '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and  O.FechaVto >  '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and  O.TipoOp In ('MMFRN','MMFCMV') ")
SQL.ADD(" and  O.Vehiculo1 In ("~Pipe2Comillas(Dialogo.Lista1)~") ")
SQL.ADD(" and  O.Especie Not In ("~val(en1)~") ")
SQL.ADD(" Union All ")
SQL.ADD("Select O.Vehiculo1, O.Especie, O.FechaVto Fecha, O.NrOperacion, O.TipoOp, ")
SQL.ADD("TotalNetoCli3/1000 Cant from "~DBO~".OPERACIONES  O ")
SQL.ADD("where O.FechaOp  <= '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and  O.FechaVto >  '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and  O.TipoOp In ('MMON') ")
SQL.ADD(" and  O.Vehiculo1 In ("~Pipe2Comillas(Dialogo.Lista1)~") ")
SQL.ADD(" and  O.Especie Not In ("~val(en1)~") ")

SQL.ADD("order by 1, 2 ")

act(u:200,' ')
Recorrer SQL
  if Not EqStr(val(u:200),fbn['Vehiculo1'])
     Recorrer&i 3, val(b:200)
        if EqStr(fbn['Vehiculo1'],val(&i:2))
           acn(e:200,&i)
           Break
        endif
     Proximo
     act(u:200,fbn['Vehiculo1'])
  endif

  If Not EqStr(val(aa:200),fbn['Especie'])
     *- Cotizacion
     ACN(zz2, CotizacionC(fbn['Especie'],,,,Dialogo.Fecha1,1,SI,Val(me1))) 
     ACT(aa:200, fbn['Especie'] ) 
  Endif


  if EqStr(fbn['Especie'],'ARP')
     scn(val(e:200):val(f:200),fbn['Cant'])
     scn(val(e:200):val(i:200),fbn['Cant'])
     SetFont(val(e:200):val(f:200)..val(e:200):val(f:200),3)
    else
     scn(val(e:200):val(g:200),fbn['Cant']*Val(zz2))
     scn(val(e:200):val(l:200),fbn['Cant']*Val(zz2))
     SetFont(val(e:200):val(g:200)..val(e:200):val(g:200),3)
  endif
  if fbn['Fecha'] <= (Dialogo.Fecha1+365)
     scn(val(e:200):val(v:200),fbn['Cant']*Val(zz2))
     scn(b:val(v:200),fbn['Cant']*Val(zz2))
     SetFont(val(e:200):val(v:200)..val(e:200):val(v:200),3)
    else
     if Not Incluido(fbn['TipoOp'],'MMONM')
        scn(val(e:200):val(w:200),fbn['Cant']*Val(zz2))
        scn(b:val(w:200),fbn['Cant']*Val(zz2))
        SetFont(val(e:200):val(w:200)..val(e:200):val(w:200),3)
       else
        SQL.ADD("Select Sum(Cantidad4)/1000 Parcial from "~DBO~".CUOTAS ")
        SQL.ADD("where  NrOperacion = '"~fbn['NrOperacion']~"' ")
        SQL.ADD("  and  Fecha2 <= '"~IdxDate(Dialogo.Fecha1+365)~"' ")
        SQL.ADD("  and  Fecha2 >  '"~IdxDate(Dialogo.Fecha1)~"' ")

        acn(y:200,0)
        Recorrer SQL
          acn(y:200,fbn['Parcial'])
        Proximo

        scn(val(e:200):val(v:200),val(y:200)*Val(zz2))
        scn(b:val(v:200),val(y:200)*Val(zz2))
        SetFont(val(e:200):val(v:200)..val(e:200):val(v:200),3)
        scn(val(e:200):val(w:200),(fbn['Cant']-val(y:200))*Val(zz2))
        scn(b:val(w:200),(fbn['Cant']-val(y:200))*Val(zz2))
        SetFont(val(e:200):val(w:200)..val(e:200):val(w:200),3)
     endif
  endif
proximo
