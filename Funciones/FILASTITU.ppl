ACN(fi20, Val(fi0))

SQL.ADD("select Distinct Especie  from "~DBO~".SALDOS")
SQL.ADD("WHERE Saldo >= 0 ")
SQL.ADD("      and "~DBO~".DesciendeDeEspecie(Especie, 'TITULO')=1 ")
SQL.ADD("      and NrSaldo = 2")

Recorrer SQL
   ACT(a:Val(fi20), FBN['Especie'])
   SCN(fi20, 1)
Proximo
