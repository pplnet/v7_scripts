let &diasCont SQL("Select 1 from "~DBO~".VARIABLES where Codigo = 'DIASCONT' ")
if &diasCont=1
   let &diasCont Trunc(Var('DIASCONT'),0)
endif
if &diasCont<1
   let &diasCont 1
endif
ACD(x2, Habiles(PriDeMes(Fsys,SI)-1,&diasCont,'ARG'))

let &fechaCierre val(x2)