CrearDialogo
  Especie1  : 'Especie'  ;
  Corredor1 : 'Corredor' ;
  Cliente1  : 'Cliente'  ;
  Operador  : 'Operador' ;;;;;;;;;UsuarioActivo
  Vehiculo1 : 'Vehiculo' ;;;;;;;;NO;VAR('VEHICULODE')
FinDialogo

if not Ok 
  Cancelar
endif

CrearFont(1, 'Arial,8,B')
CrearFont(2, 'Arial,8,')

ACT(a:4, 'Resumen de Operaciones')
ACT(a:5, 'Fecha : '~Fecha(FSys))
if NoVacio(Dialogo.Vehiculo1)
  ACT(a:6, 'Vehiculo : '~Dialogo.Vehiculo1)
else
  ACT(a:6, 'Vehiculo : Todos')
endif
ACT(z8, " TipoOp in ('TIC','TIV','IC','IV', 'TIFC', 'TIFV', 'TIPA', 'TIPP','TIADJ','TIRES') and FechaOp = '"~IdxDate(FSys)~"'")
if NoVacio(Dialogo.Especie1)
  ACT(a:7, 'Especie='~Dialogo.Especie1~' ')
  ACT(z8, Val(z8)~" and Especie = '"~Dialogo.Especie1~"'")
endif
if NoVacio(Dialogo.Corredor1)
  ACT(a:7, Val(a:7)~'Corredor='~Dialogo.Corredor1~' ')
  ACT(z8, Val(z8)~" and Corredor = '"~Dialogo.Corredor1~"'")
endif
if NoVacio(Dialogo.Cliente1)
  ACT(a:7, Val(a:7)~'Cliente1='~Dialogo.Cliente1~' ')
  ACT(z8, Val(z8)~" and Cliente1 = '"~Dialogo.Cliente1~"'")
endif
if NoVacio(Dialogo.Operador)
  ACT(a:7, Val(a:7)~'Operador='~Dialogo.Operador~' ')
  ACT(z8, Val(z8)~" and Operador = '"~Dialogo.Operador~"'")
endif

if NoVacio(Val(a7))
  ACT(a:7, 'Criterio : '~Val(a:7))
else
  ACT(a:7, 'Criterio : Todas las operaciones')
endif

ACT(a:10, 'Tipo Op.')
ACT(b:10, 'Especie')
ACT(c:10, 'V.Nominal')
ACT(d:10, 'Precio')
ACT(e:10, 'Cliente')
ACT(f:10, 'Corredor')
ACT(g:10, 'Comision')
ACT(h:10, 'Operador')
ACT(i:10, '# Operacion')
AlinearCeldas(c:10..d:10, 'D')
AlinearCeldas(g:10..g:10, 'D')
SetFont(a:1..h:10, 1)
AgregarMarco(a:10..i:10,2,0,Exe('COLOR'))
SetFilaActual(10)

SQL.ADD("Select TipoOp, Especie, Cantidad, Precio1, Cliente1, Corredor, TotalComisiones, Operador, NrOperacion from "~DBO~".OPERACIONES where ")
SQL.ADD(Val(z8))
if NoVacio(Dialogo.Vehiculo1)
  SQL.ADD(" and Vehiculo1 = '"~Dialogo.Vehiculo1~"' ")
endif

recorrer SQL
  ACT(a:fac, fbn('TipoOp'))
  ACT(b:fac, fbn('Especie'))
  ACN(c:fac, fbn('Cantidad'))
  ACN(d:fac, fbn('Precio1'))
  ACT(e:fac, fbn('Cliente1'))
  ACT(f:fac, fbn('Corredor'))
  ACN(g:fac, fbn('TotalComisiones'))
  ACT(h:fac, fbn('Operador'))
  ACT(i:fac, fbn('NrOperacion'))
  ACLO(i:fac, fbn('NrOperacion'))
proximo

SetFont(a:11..i:fac, 2)
*SetFont(a:Val(z2)+1..h:fac, 2)
FormatoCeldas(c1..c:fac, Exe('F2'))
FormatoCeldas(d1..d:fac, Exe('F6'))
FormatoCeldas(g1..g:fac, Exe('F2'))

ACO(2,8)
ACO(4,8)
ACO(5,8)
ACO(6,8)
ACO(7,8)
ACO(8,8)
ACO(9,8)

SetFont(a1..i:10, 1)
LimitesVision(j:fac+1)
CrearFont(6,"Arial,6,B")
RangoTitulo(a:10..j:FAC, 1)