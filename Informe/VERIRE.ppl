CrearDialogo  
  Fecha1     : 'Fecha'                 ;;;;;;;;;Fsys
FinDialogo

SQL.ADD("Select count(*) as Cuantos from "~DBO~".OBJETOS where Codigo='INVRES' ")
SQL.ADD(" and Extension='"~Dialogo.Fecha1~"' ")

Recorrer SQL

   If FBN['Cuantos']=0
      Cancelar('No existe el reporte solicitado')
   else
      LeerGrilla('INVRES', Dialogo.Fecha1)
   Endif
Proximo

Impresion(1,2)
* LimitesVision(val(b:200)+2:130)


