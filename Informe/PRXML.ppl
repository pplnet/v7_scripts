XML.Iniciar()
XML.CargarArchivo("C:\Users\CLOMAS\Desktop\cargill.xml")
xml.StartRecorrer("//Stmt")

*prueba

XML.CargarSQL("INSERT INTO OPERACIONESBITS (NrOperacion, NrBit, Valor) VALUES ('00000002','1','0')")

while
    
    if not(XML.MoveNext())
        break
    endif

    XML.StartRecorrer("//Stmt[Acct/Id/Othr[Id=1901517079]]")
    
    while
    
        if not(XML.MoveNext())
            break
        endif
        
        MessageBox(XML.Fbn("CreDtTm"))
        
    next
    
    XML.Pop()

next

XML.Pop()