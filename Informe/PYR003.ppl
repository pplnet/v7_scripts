CrearDialogo
  Fecha1:'Fecha';;;;;;;;;FSys
  Vehiculo1:'Vehiculo'
  Book1:'Book'
  Especie1:'Especie'
FinDialogo

if not OK 
  Cancelar
endif

ACT(a1, 'Detalle de Posicion')
ACT(a2, 'Fecha: '~Dialogo.Fecha1)
ACT(a3, 'Especie: '~Dialogo.Especie1)
ACT(a4, 'Vehiculo: '~Dialogo.Vehiculo1)
ACT(a5, 'Book: '~Dialogo.Book1)

CrearFont(1,"Arial,8,B")
CrearFont(2,"Arial,8,")

SetFont(a1..z5, 1)
SetFilaActual(6)

ACT(b7, 'Cantidad')
ACT(c7, 'PPP')
*ACT(d7, 'PPReseteado')
*ACT(e7, 'Intereses')

ACT(d7, 'Cantidad')
ACT(e7, 'PPHistorico')
ACT(f7, 'PPReseteado')
ACT(g7, 'Intereses')


ACT(a8, 'Inicio')
ACT(a9, 'Compras')
ACT(a10, 'Ventas')
ACT(a11, 'Aumentos')
ACT(a12, 'Disminuc')
ACT(a13, 'Aumentos')
ACT(a14, 'Disminuc')
ACT(a15, 'Fin')

ACT(a16, 'Prest.Otorg.')
ACT(a17, 'Prest.Recib.')
ACT(a18, 'Pase Activo')
ACT(a19, 'Pase Pasivo')
ACT(a20, 'Garant.Otorg.')
ACT(a21, 'Garant.Recib.')



SQL.ADD("Select * from "~DBO~".POSICIONES2 where")
SQL.ADD("Fecha='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("and Especie='"~Dialogo.Especie1~"'")
SQL.ADD("and Vehiculo='"~Dialogo.Vehiculo1~"'")
SQL.ADD("and Book='"~Dialogo.Book1~"'")

recorrer SQL
  if fbn('NrSaldo')=1
    ACN(b8, fbn('CantidadIni'))
    ACN(b9, fbn('Compras'))
    ACN(b10, fbn('Ventas'))
    ACN(b11, fbn('Aumentos'))
    ACN(b12, fbn('Dismin'))
    ACN(b13, fbn('AumentosCD'))
    ACN(b14, fbn('DisminCD'))
    ACN(b15, fbn('CantidadFin'))
    ACN(b16, fbn('PmosOtorgados'))
    ACN(b17, fbn('PmosTomados'))
    ACN(b18, fbn('PasesActivos'))
    ACN(b19, fbn('PasesPasivos'))
    ACN(b20, fbn('GarantiasEntregadas'))
    ACN(b21, fbn('GarantiasRecibidas'))

    ACN(c8, fbn('PIHME'))
    ACN(c9, fbn('PCME'))
    ACN(c10, fbn('PVME'))
    ACN(c11, fbn('PAME'))
    ACN(c12, fbn('PDME'))
    ACN(c13, fbn('PA2ME'))
    ACN(c14, fbn('PD2ME'))
    ACN(c15, fbn('PFHME'))

*    ACN(d8, fbn('PIRME'))
*    ACN(d15, fbn('PFRME'))
*    ACN(e8, fbn('IIME'))
*    ACN(e15, fbn('IFME'))
    
  else
    ACN(d8, fbn('CantidadIni'))
    ACN(d9, fbn('Compras'))
    ACN(d10, fbn('Ventas'))
    ACN(d11, fbn('Aumentos'))
    ACN(d12, fbn('Dismin'))
    ACN(d13, fbn('AumentosCD'))
    ACN(d14, fbn('DisminCD'))
    ACN(d15, fbn('CantidadFin'))
    ACN(d16, fbn('PmosOtorgados'))
    ACN(d17, fbn('PmosTomados'))
    ACN(d18, fbn('PasesActivos'))
    ACN(d19, fbn('PasesPasivos'))
    ACN(d20, fbn('GarantiasEntregadas'))
    ACN(d21, fbn('GarantiasRecibidas'))

    ACN(e8, fbn('PIHME'))
    ACN(e9, fbn('PCME'))
    ACN(e10, fbn('PVME'))
    ACN(e11, fbn('PAME'))
    ACN(e12, fbn('PDME'))
    ACN(e13, fbn('PA2ME'))
    ACN(e14, fbn('PD2ME'))
    ACN(e15, fbn('PFHME'))

    ACN(f8, fbn('PIRME'))
    ACN(f15, fbn('PFRME'))
    ACN(g8, fbn('IIME'))
    ACN(g15, fbn('IFME'))
  endif
proximo

FormatoCeldas(c:8..c:21, Exe('F7'))
FormatoCeldas(e:8..g:21, Exe('F7'))

SetFont(a1..z22, 1)
SetFont(b8..z22, 2)

AgregarMarco(a:7..g7,1,0,Exe('COLOR'))
AgregarMarco(a:8..g21,1)
AgregarMarco(b:8..c21,1)

LimitesVision(h22)
SetPrintFit(3,100)
Impresion(1,1)
Impresion(2,1)
Impresion(4,1)
SetMargins(0,0,0,2)

ACO(1, 10)
ACO(2, 10)
ACO(3, 10)
ACO(4, 10)
ACO(5, 10)
ACO(6, 10)
ACO(7, 10)
ACO(8, 10)
ACO(9, 10)
