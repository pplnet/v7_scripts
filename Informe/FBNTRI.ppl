
CrearDialogo
    NrOperacion1: '# Operacion :'
FinDialogo


if not ok
  Cancelar
endif

SQL.ADD("Select * ")
SQL.ADD("from "~DBO~".OPERACIONES    ")
SQL.ADD("where NrOperacion = '"~Dialogo.NrOperacion1~"' ")

Recorrer SQL

  
   if incluido(fbn('TipoOp'),'FCR|FCS|MMCA|MMPF|MMPFP|MMCO|MMCT|MMAD|MMDES|MMPO')
    MessageBox ("Entro en  incluido FCS'"~fbn('TipoOp')~"' ")
    *  Cancelar
  endif
  
  
   if not incluido(fbn('TipoOp'),'FCR|FCS|MMCA|MMPF|MMPFP|MMCO|MMCT|MMAD|MMDES|MMPO')
    MessageBox ("Entro en No incluido FCS'"~fbn('TipoOp')~"' ")
    *  Cancelar
  endif
  Proximo