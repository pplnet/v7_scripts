


* Comentado pq no existen las tablas del Select


*********************************************************************
**
** Lista los Codigos de Relaciones Interfase Contable
**
**********************************************************************
*HojaVisible(SI)
*ModoReproceso(ConDialogo)
*CrearDialogo
** TipoOp1     : "Tipo Oper."  ;;;;;;
*  Lista2      : "Tipo Oper."  ;  ;1;;;;;;;;;SQLSet(" Select Codigo From "~DBO~".TIPOSOPERACION ");
*  String1     : "Origen"      ;;;;;;
*FinDialogo
*
*if not Ok
*  Cancelar
*endif
*
*CrearFont(1, 'Arial,8,B')
*CrearFont(2, 'Arial,8,')
*CrearFont(3, 'Arial,10,B')
*
*SetFilaActual(1)
*ACT(a:1,'Posicion Bancaria - CONFIDENCIAL')
*SetFont(a:1,1)
*
*SetFilaActual(fac+2)
*ACT(b:fac, 'Listado de Codigos de Interfase Contable')
*SetFont(f:fac, 3)
*
*SetFilaActual(fac+1)
**if NoVacio(Dialogo.TipoOp1)
**  ACT(a:fac+1, 'Tipo Oper.: '~Dialogo.TipoOp1)
*If NoVAcio(Dialogo.Lista2)
*   ACT(a:fac+1, 'Tipo Oper.: '~iif(vacio(Dialogo.Lista2),'Todas',Pipe2Comillas(Dialogo.Lista2)))
*else
*   ACT(a:fac+1, 'Tipo Oper: Todas')
*endif
*SetFilaActual(fac+1)
*if NoVacio(Dialogo.String1)
*  ACT(a:fac+1, 'Origen: '~Dialogo.String1)
*else
*  ACT(a:fac+1, 'Origen: Todos')
*endif
*
*SetFilaActual(fac+2)
*ACT(A:fac,'Tipo Op.')
*ACT(B:fac,'Descripcion')
*ACT(C:fac,'Condicion Envio')
*ACT(D:fac,'Concepto FPA')
*ACT(E:fac,'Concepto')
*ACT(F:fac,'Descrip.')
*ACT(G:fac,'Origen')
*ACT(H:fac,'Descr.')
*ACT(I:fac,'Instante')
*ACT(J:fac,'Descr.')
*ACT(K:fac,'SubInstante')
*ACT(L:fac,'Descr.')
*ACT(cd1,'Codigo Sin Tipificar')
*
**--  Origen
*ACT(da1, TRIM(MIRT(Dialogo.String1))) 
*AgregarMarco(a:fac..l:fac,1,0,Exe('COLOR'))
*Setfont(a2..l:fac, 1)
*AlinearCeldas(j:fac..l:fac, 'C')
**--  Select
*SQL.ADD(" Select A.TipoOp, A.Nombre, A.Condicion, A.ConceptoFPA, A.ICConcepto, A.ICOrigen, A.ICInstante   ")
*SQL.ADD(" c.Descripcion Concepto, o.Descripcion Origen, i.Descripcion Instante, s.Descripcion SubInstante, t.nombre")
*SQL.ADD("   from "~DBO~".ICRELACIONES A, ")
*SQL.ADD("        "~DBO~".ICCONCEPTOS C, "~DBO~".ICORIGENES O, "~DBO~".ICINSTANTES I, "~DBO~".ICSUBINSTANTES S,     ")
*SQL.ADD("        "~DBO~".TIPOSOPERACION T ")
*SQL.ADD("   where A.TipoOp        *= T.Codigo  ")
*SQL.ADD("     and A.ICOrigen      = O.Codigo ")
*SQL.ADD("     and A.ICInstante    = I.Codigo ")
*SQL.ADD("     and A.ICSubInstante = S.Codigo ")
*SQL.ADD("     and A.ICConcepto    = C.Codigo ")
**if NoVacio(Dialogo.TipoOp1)
**   SQL.ADD("     and A.TipoOp = '"~Dialogo.TipoOp1~"' ")
*If NoVAcio(Dialogo.Lista2)
*   SQL.ADD("     and A.TipoOp  IN ("~Pipe2Comillas(Dialogo.Lista2)~")  ")
*Endif
*If NoVacio(Dialogo.String1)
*   SQL.ADD("and A.ICOrigen like '"~val(da1)~"%' ")
*Endif
*SQL.ADD(" order by A.ICOrigen, A.ICINSTANTE, A.ICSUBINSTANTE")
*acn(z3,0)
*acn(z2,0)
*ACN(FI:1, fac+1)
*Recorrer SQL
*   ACT(a:fac,FBN['TipoOp'])
*   ACT(b:fac,FBN['Nombre'])
*   ACT(c:fac,FBN['Condicion'])
*   ACT(d:fac,FBN['ConceptoFPA'])
*   ACT(e:fac,FBN['ICConcepto'])
*   ACT(f:fac,FBN['Concepto'])
*   ACT(g:fac,FBN['ICOrigen'])
*   ACT(h:fac,FBN['Origen'])
*   ACT(i:fac,FBN['ICInstante'])
*   ACT(j:fac,FBN['Instante'])
*   ACT(k:fac,FBN['ICSubInstante'])
*   IF NOT EQSTR('00',FBN['ICSubInstante']) 
*      ACT(l:fac,FBN['SubInstante'])
*   endif
*   SetFont(a:fac..l:fac, 2)
*Proximo
*Ifa
*Ifa
*AgregarMarco(a:fac..l:fac,1,0,Exe('COLOR'))
*SetFont(a:fac..l:fac, 1)
*Impresion(1,2)
*act(a:fac+2,'FIN DE LISTADO')
*SetFont(a:fac..a:fac, 1)
*SetFilaActual(fac+1)
*LimitesVision(l:fac+1)
*ACO(2, 25)
*ACO(3, 25)
*ACO(4, 25)
*ACO(6, 15)
*ACO(8, 25)
*ACO(10, 20)
*ACO(12, 10)
*SetPrintFit(3,75)
*RangoTitulo(a:1..l:fac+1, 7)