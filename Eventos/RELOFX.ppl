* Si es un repo de monedas diferentes a arp llama al evento que genera arbitraje
* caso contrario llama al evento que genera compra venta

SQL.ADD(" Select * from "~DBO~".OPERACIONES T WHERE TipoOp in ('FXREPO','FXREPV') ")
SQL.ADD(" and (RB1='0' or RB1 IS NULL) ")
SQL.ADD(" ORDER BY NrOperacion ")

Recorrer SQL   
    Message('*-- Procesando '~FBN['NrOperacion'])
    If EqStr(FBN['Especie'],'ARP') o EqStr(FBN['ContraEspecie'],'ARP')
       EjecutarEvento('GCVREP',NrOperacion1=FBN['NrOperacion'])    
    Else
       EjecutarEvento('GARREP',NrOperacion1=FBN['NrOperacion'])
    EndIf
Proximo