CrearDialogo
  NrOrden1:     'Nr. Operacion'     ;1;1;1;  ;  ;Novacio(Dialogo.NrOrden1);'Orden Invalida.';NO;
  Check1:       'Procesa'           ; ; ;1;SI;SI;;;;1;
  Check2:       'Borra Excep.'      ; ; ;1;SI;SI;;;;1;
  Check3:       'Genera Instancias' ; ; ;1;SI;SI;;;;1;
FinDialogo
if not Ok
  Cancelar
endif

if Dialogo.Check1=1
  if Dialogo.Check2=1
  SQL.ADD("Delete "~DBO~".EXCEPCIONES WHERE NrOperacion='"~Dialogo.NrOrden1~"' ")
    SQL.NEW
    SQL.EXEC
  endif
  if Dialogo.Check3=1
     ModificarOrden(Dialogo.NrOrden1,0,SI,SI,SI,NO,SI,NO,NO,NO,NO,NO,NO,NO,NO,NO,NO,SI,NO,NO,NO,SI,NO,SI,NO,0,NO,NO)
  else
     ModificarOrden(Dialogo.NrOrden1,0,NO,SI,SI,NO,SI,NO,NO,NO,NO,NO,NO,NO,NO,NO,NO,SI,NO,NO,NO,SI,NO,SI,NO,0,NO,NO)
  endif
endif

  