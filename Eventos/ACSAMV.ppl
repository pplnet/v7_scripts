CrearDialogo
    Fecha1      : 'Fecha'      ;;;;;;;;;FSYS
    Especie1    : 'Especie'    ;;;;;;NOVACIO(Dialogo.Especie1);"Ingrese una Especie";NO;
FinDialogo

If NOT OK
    Cancelar
endif


SQL.ADD("SELECT FechaMov, Especie, NrSaldo FROM "~DBO~".MOVPENDIENTES")
SQL.ADD("WHERE NrSaldo = -29")
SQL.ADD("AND FechaMov = '"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("AND Especie = '"~Dialogo.Especie1~"'")


Recorrer SQL

    SQL.ADD("UPDATE "~DBO~".MOVPENDIENTES")
    SQL.ADD("SET NrSaldo = 1")
    SQL.ADD("WHERE Especie = '"~Dialogo.Especie1~"'")
    SQL.ADD("AND FechaMov = '"~IdxDate(Dialogo.Fecha1)~"'")
    SQL.ADD("AND NrSaldo = -29")
    SQL.NEW()

proximo

SQL.EXEC