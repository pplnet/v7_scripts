*Ignore=V7
test('funciones de formato', ->{
    AgregarMArco(A:1, -1, 0, RGB(250,240,230))
    assertPass()
    SetFont(A:2..A:3, 1)
    AgregarMarco(A:2..A:3, 0)
    assertPass()
    AgregarMarco(1:FAC..7:FAC, 1, 0, RGB(209,209,209))
    assertPass()
    AgregarMarco(1:FAC..7:FAC, -1, 0, RGB(209,209,209))
    assertPass()
    RangoTitulo(A:1..D:FAC, Val(W:1) + 10)
    assertPass()
})
