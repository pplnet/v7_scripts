SQL.ADD(" Insert Into "~DBO~".MOVEJECUTADOS ")
SQL.ADD(" Select * From "~DBO~".MOVPENDIENTES ")
SQL.ADD(" Where NrOperacion = '"~Val(PA:1)~"' ")
SQL.ADD(" AND Cliente = '"~Val(PA:2)~"' ")
SQL.ADD(" AND Concepto = '"~Val(PA:3)~"' ")
SQL.ADD(" AND Mercado = '"~Val(PA:4)~"' ")
SQL.ADD(" AND NrSaldo = 4 ")
SQL.NEW


SQL.ADD(" Update "~DBO~".MOVEJECUTADOS Set ")
SQL.ADD(" FechaEje = '"~IdxDate(IIF(Dialogo.Radio1=0,Dialogo.Fecha2,Fsys))~"' ")
SQL.ADD(" Where NrOperacion = '"~Val(PA:1)~"' ")
SQL.ADD(" AND Cliente = '"~Val(PA:2)~"' ")
SQL.ADD(" AND Concepto = '"~Val(PA:3)~"' ")
SQL.ADD(" AND Mercado = '"~Val(PA:4)~"' ")
SQL.ADD(" AND NrSaldo = 4 ")
SQL.NEW

SQL.ADD(" Delete "~DBO~".MOVPENDIENTES ")
SQL.ADD(" Where NrOperacion = '"~Val(PA:1)~"' ")
SQL.ADD(" AND Cliente = '"~Val(PA:2)~"' ")
SQL.ADD(" AND Concepto = '"~Val(PA:3)~"' ")
SQL.ADD(" AND Mercado = '"~Val(PA:4)~"' ")
SQL.ADD(" AND NrSaldo = 4 ")
SQL.NEW