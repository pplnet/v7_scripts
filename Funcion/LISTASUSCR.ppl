HojaVisible(SI) 

CrearFont(1,"Arial,8,")
CrearFont(2,"Arial,8,B")
CrearFont(3,"Arial,10,B")

SetFilaActual(1)
ACT(a:FAC,' Fecha de Proceso '~Dialogo.Fecha1 )
SetFont(a:FAC,1)


SetFilaActual(2)


ACT(a:FAC,' Suscripciones a adjudicar para la especie '~Dialogo.Especie1 )
SetFont(a:FAC,3)

SetFilaActual(4)
ACT(a:FAC,'Nro.Orden')
ACT(b:FAC,'Cliente ')
ACT(c:FAC,'Fecha')
ACT(d:FAC,'Hora')
ACT(e:FAC,'Postura')
ACT(f:FAC,'Cantidad' )

AlinearCeldas(a:FAC..h:FAC,'C')
SetFont(a:FAC..h:FAC,2)
AgregarMarco(a:FAC..f:FAC,2,0,Exe('COLOR'))

ifa
ifa

acn(ma1,0)


Recorrer&i Val(pp11),Val(pp1)
       
    if Val(a:&i) = 1     
       
       if Val(ma1) = 0
          act(c:Fac, ' Precio de Corte ' )
          act(e:Fac, Val(r:&i) )
          SetFont(a:FAC..e:FAC,1) 
          AgregarMarco(c:FAC..e:FAC,2,0)
          FormatoCeldas(e:fac, Exe('F6'))
          acn(ma1,1)

          ifa
          ifa
       endif

       act(a:fac,Val(b:&i) )
       act(b:fac,Val(e:&i) )
       act(c:fac,Val(c:&i) )
       act(d:fac,Val(g:&i) )  
       acn(e:fac,Val(q:&i) )
       acn(f:fac,Val(ch:&i) )

   
       SetFont(a:FAC..h:FAC,1) 
       FormatoCeldas(e:fac..e:fac, Exe('F6'))
       ifa

    endif

Proximo

ifa
act(b:fac,'Total de Suscripciones a ADJUDICAR ')
acn(f:fac,Sum(f:3..f:fac) )
AgregarMarco(b:FAC..f:FAC,2,0)
SetFont(b:FAC..h:FAC,1) 


ifa
ifa
ACT(a:FAC,' Suscripciones NO adjudicadas  para la especie '~Dialogo.Especie1 )
SetFont(a:FAC,3)

ifa
ACT(a:FAC,'Nro.Orden')
ACT(b:FAC,'Cliente ')
ACT(c:FAC,'Fecha')
ACT(d:FAC,'Hora')
ACT(e:FAC,'Postura')
ACT(f:FAC,'Cantidad' )

AlinearCeldas(a:FAC..h:FAC,'C')
SetFont(a:FAC..h:FAC,2)

AgregarMarco(a:FAC..f:FAC,2,0,Exe('COLOR'))

ifa
ifa
acn(fd1,fac)

Recorrer&i Val(pp11),Val(pp1)

  if Val(a:&i) = 0     
     act(a:fac,Val(b:&i) )
     act(b:fac,Val(e:&i) )
     act(c:fac,Val(c:&i) )
     act(d:fac,Val(g:&i) )  
     acn(e:fac,Val(q:&i) )
     acn(f:fac,Val(ch:&i) )
     SetFont(a:FAC..h:FAC,1) 
     ifa
  endif

Proximo




ifa
act(b:fac,'Total de Suscripciones NO ADJUDICADAS ')
acn(f:fac,Sum(f:Val(fd1)..f:fac) )
AgregarMarco(b:FAC..f:FAC,2,0)
SetFont(b:FAC..h:FAC,1) 

ifa
ifa

aco(1,10)
aco(2,10)
aco(5,10)
aco(6,10)



LimitesVision(h:Fac)

