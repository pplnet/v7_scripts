**-----------------------------------------------------**
** AUXCOPIAT2 - Copiativo Boletos  Función Auxiliar     **
**-----------------------------------------------------**

        ** poner titulos MAV
        ifa
        ACT(j:FAC+1, val(xx1))
        if Dialogo.Fecha2>Dialogo.Fecha1
           ACT(j:FAC+2, 'Registro de Operaciones Cheques desde '~Dialogo.Fecha1~' hasta '~Dialogo.Fecha2 )
        else
           ACT(j:FAC+2, 'Registro de Operaciones Cheques del '~Dialogo.Fecha1)
        endif        
        
        AlinearCeldas(j:FAC+1..j:FAC+2,'C')


        SetFilaActual(fac+4)

        *ACT(a:FAC, 'Cliente ')
        *ACT(c:FAC, 'Nro.CUIT/CUIL')
        *ACT(e:FAC, 'Especie')
        *ACT(f:FAC, 'Plazo')
        *ACT(g:FAC, 'Moneda')
        *ACT(h:FAC, 'Cant. Comprada')
        *ACT(i:FAC, 'P.Prom. Compras')
        *ACT(j:FAC, 'Monto Compras')
        *ACT(k:FAC, 'Cant. Vendida')
        *ACT(l:FAC, 'P.Prom. Ventas')
        *ACT(m:FAC, 'Monto Ventas')

        *AlinearCeldas(a:FAC..k:fac,'C')
        *AgregarMarco(a:FAC..k:fac,2)
        *SetFont(a:FAC..k:fac,2)
        *ifa
        **ACN(aa:1,FAC+1)
        *ACN(aw1, 5)
        
        ACT(a:FAC, 'Boleto')
        ACT(b:FAC, 'Nro Minuta')
        ACT(c:FAC, 'Fecha Op.')
        ACT(d:FAC, 'Fecha Liq.')
        ACT(e:FAC, 'Tipo Op')
        ACT(f:FAC, 'Plazo')
        ACT(g:FAC, 'Especie')
        ACT(h:FAC, 'Cliente - Nro.CUIT/CUIL')
        ACT(i:fac, 'Moneda')
        ACT(j:fac, 'Compras')
        ACT(k:fac, 'P.Prom. Compras')
        ACT(l:fac, 'Monto Compras')
        ACT(m:fac, 'Ventas')
        ACT(n:fac, 'P.Prom. Ventas')
        ACT(o:fac, 'Monto Ventas')

        AlinearCeldas(a:FAC..k:fac,'C')
        AgregarMarco(a:FAC..o:fac,2)
        SetFont(a:FAC..o:fac,2)
        ifa
        **ACN(aa:1,FAC+1)
        ACN(aw1, 5)
