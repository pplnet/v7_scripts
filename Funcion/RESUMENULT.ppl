* --- Actualiza en la cuenta la ultima Fecha de Emision de Resumen
If Dialogo.Check1=1 
   SQL.ADD("UPDATE "~DBO~".CUENTAS ")
   SQL.ADD("   SET FechaUltResumen="~IdxDate2(val(x3)))
   SQL.ADD(" WHERE Codigo='"~FBN['Codigo']~"' ")
   SQL.NEW
   SQL.EXEC
Endif
