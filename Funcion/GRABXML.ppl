** Funcion para Armado XML
def xmlElemento(nombre, valor, cierre)
 if EqStr(valor,'XxXxXxX')
    if EqStr(Cierre,'SI')
       return '</'~nombre~'>'
    else
       return '<'~nombre~'>'
    endif
 else
    return '<'~nombre~'>'~valor~'</'~nombre~'>'
 end
end