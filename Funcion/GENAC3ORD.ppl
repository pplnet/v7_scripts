                 ACT(w7, GetNumerador(220))
                 ACT(c:FAC, Val(w7))
                 Message('--- #Or : '~Val(w7)~' Tipo : '~Val(t:2)~' Especie : '~Val(t:3)~' Cliente : '~Val(t:56))
                 SQL.ADD("Insert into "~DBO~".ORDENES (")
               
                 SQL.ADD("NrOrden, ")
                 SQL.ADD("TipoOrden, ")
                 SQL.ADD("Especie, ")
                 SQL.ADD("Cliente, ")
                
                 SQL.ADD("CantidadTotalOrden, ")
                 SQL.ADD("ContraEspecie, ")
                 SQL.ADD("PrecioLimite, ")
                 
                 SQL.ADD("Cupon1,")
                 SQL.ADD("FechaOrden, ")
                 SQL.ADD("Lotes, ")
                 SQL.ADD("RB5, ")
                 SQL.ADD("Plazo1, ")
                 
                 SQL.ADD("FechaVto, ")
                 SQL.ADD("Mercado, ")
                 SQL.ADD("Mercado2, ")
                 
                 SQL.ADD("TablaFeriados1, ")
                 SQL.ADD("Operador, ")
                 SQL.ADD("Operador1, ")
                 SQL.ADD("UDN1, ")
                
                 SQL.ADD("Vehiculo, ")
                 SQL.ADD("Mercado3, ")
                 SQL.ADD("NroAltaMAE, ")
                 SQL.ADD("RuedaMAE, ")
                 
                 SQL.ADD("AceptaMAE, ")
                 SQL.ADD("FechaAltaMAE, ")
                 SQL.ADD("SecEnvioMAE, ")
                  
                 SQL.ADD("FechaEnvioMAE, ")
                 SQL.ADD("Cuenta1, ")
                 SQL.ADD("Cuenta4, ")
                 SQL.ADD("Cuenta5, ")
                 SQL.ADD("Numerador, ")
                
                 SQL.ADD("FechaCarga, ")
                 SQL.ADD("FechaCargaSys, ")
                 SQL.ADD("UsrInput, ")
                 SQL.ADD("Direccion, ")
                 
                 SQL.ADD("HoraOrden, ")
                 SQL.ADD("RB1, ")
                 SQL.ADD("RB2, ")
                 SQL.ADD("Canal1, ")
                 
                 SQL.ADD("AfectaLS, ")
                 SQL.ADD("MB1, ")
                 ** FJL 07/05/21 - # 703 Ordenes no bloquean
                 SQL.ADD("MB2, ")
                 SQL.ADD("RB9, ")
                 SQL.ADD("RB10 ")
               
                 SQL.ADD(") values (")
               
                 * NrOrden
                 SQL.ADD(" '"~Val(w7)~"', ")
                 * TipoOp
                 SQL.ADD(" '"~Val(u:2)~"', ")
                 * Especie
                 SQL.ADD(" '"~Val(t:3)~"', ")
                 * Cliente
                 SQL.ADD(" '"~Val(t:56)~"', ")
                                 
                 * CantidadTotalOrden
                 SQL.ADD(" "~FStr(Val(t:11),0,2)~", ")
                 * ContraEspecie
                 SQL.ADD(" '"~Val(t:12)~"', ")
                 * PrecioLimite
                 SQL.ADD(" "~FStr(Val(t:13),0,8)~", ")
                 * Cupon1
                 SQL.ADD(" "~Val(t:17)~", ")
                 * FechaOrden
                 SQL.ADD(" '"~IdxDate(Val(t:19))~"', ")

                 * Lotes
                 SQL.ADD(" "~FStr(Val(t:21),1,0)~", ")
                 * RB5 (N)
                 SQL.ADD(" '"~Val(t:20)~"', ")
                 * Plazo1 (N)
                 SQL.ADD(" "~FStr(Val(t:21),1,0)~", ")
                 
                 * FechaVto
                 SQL.ADD(" '"~IdxDate(Val(t:45))~"', ")
                 * Mercado
                 SQL.ADD(" '"~Val(t:22)~"', ")
                 * Mercado2
                 SQL.ADD(" '"~Val(t:52)~"', ")

                 * TablaFeriados1
                 SQL.ADD(" '"~Val(t:24)~"', ")
                 * Operador
                 SQL.ADD(" '"~Val(t:25)~"', ")
                 * Operador1 OpSIOPEL
                 SQL.ADD(" '"~Val(t:26)~"', ")
              
                 * Especie Recibida no encontrada (UDN1)
                 SQL.ADD(" '"~Val(t:27)~"', ")
                 * Vehiculo
                 SQL.ADD(" '"~Val(t:29)~"', ")
                 * Mercado3
                 SQL.ADD(" '"~Val(t:31)~"', ")
               
                 * NroAltaMAE
                 SQL.ADD(" '"~Val(v:1)~"', ")
                 * RuedaMAE
                 SQL.ADD(" '"~Val(t:32)~"', ")
                
                 * AceptaMAE
                 SQL.ADD(" 'SI', ")
                 * FechaAltaMAE
                 SQL.ADD(" "~Val(t:60)~", ")
                 *SecEnvioMAE
                 SQL.ADD(" '"~Val(t:51)~"', ")
                 
                 * FechaEnvioMAE
                 SQL.ADD(" "~Val(t:49)~", ")
                 * Cuenta1
                 SQL.ADD(" '"~Val(t:28)~"', ")
                 * Cuenta4
                 SQL.ADD(" '"~Val(t:28)~"', ")
                 * Cuenta5
                 SQL.ADD(" '"~Val(t:28)~"', ")
                 * Numerador
                 SQL.ADD(" 220, ")
               
                 * FechaCarga
                 SQL.ADD(" "~SQLAhora~", ")
                 * FechaCargaSys
                 SQL.ADD(" "~IdxDate2(FSys)~", ")
                 * UsrInput
                 SQL.ADD(" '"~UsuarioActivo~"', ")
                 * Direccion
                 SQL.ADD(" 2, ")
                 
                 * HoraOrden
                 SQL.ADD(" '"~Val(t:72)~"', ")
                 * RB1
                 SQL.ADD(" '0', ")
                 * RB2
                 SQL.ADD(" '0', ")
                 * Canal (OTIC/OTIV)
                 SQL.ADD(" '"~Val(t:70)~"', ")

                 * Afecta L/S
                 SQL.ADD(" '"~Val(t:71)~"', ")                 
                 * MB1 - Generada desde SIOPEL
                 SQL.ADD(" 'SI', ")
** FJL 07/05/21 - # 703 Ordenes no bloquean
                 * MB2 - Bloquea saldos
                 SQL.ADD(" '0', ")
                 * RB9 - Forma Liquidacion 1
                 SQL.ADD(" '"~FStr(Val(t:23),1,0)~"', ")

                 * RB10 - Forma Liquidacion 2
                 SQL.ADD(" '"~FStr(Val(u:23),1,0)~"' ")
                 
                 SQL.ADD(")")
                 SQL.NEW
                 SQL.EXEC
                 
                 *Recalcula campos y Movimientos instancia 0
                 ModificarOrden(Val(w7),0,SI,SI,SI,NO,NO,NO,NO,NO,NO,NO,NO,NO,NO,NO,NO,NO,NO,NO,NO,SI,NO,NO,NO)
                 
                 ** Book default para la operacion
                 ACT(t:75,BuscarCampo('ORDENES','Book1','NrOrden',Val(w7)))
                 
                 **  Alta Ejecuciones y Operacion 
                 ACT(t:53, Val(w7))
                 ACT(w6, GetNumerador(1))
                 ACT(NR:2, GetNumerador(198))
                 ACT(c:FAC, Val(w6))
                 Message('--- #Op : '~Val(w6)~' Tipo : '~Val(t:2)~' Especie : '~Val(t:3)~' Cliente : '~Val(t:56))
                 SQL.ADD("Insert into "~DBO~".OPERACIONES (")
               
                 SQL.ADD("NrOperacion, ")
                 SQL.ADD("TipoOp, ")
                 SQL.ADD("Especie, ")
                 SQL.ADD("Cliente1, ")
                
                 SQL.ADD("Cantidad, ")
                 SQL.ADD("ContraEspecie, ")
                 SQL.ADD("Precio1, ")
                 
                 SQL.ADD("Cupon1,")
                 SQL.ADD("FechaOp, ")
                
                 SQL.ADD("Plazo, ")
                 SQL.ADD("RB5, ")
                 SQL.ADD("RB6, ")
                 SQL.ADD("FechaVto, ")
                 SQL.ADD("Mercado, ")
                 SQL.ADD("Mercado2, ")
                 SQL.ADD("FormaLiquidacion1, ")
                
                 SQL.ADD("FormaLiquidacion2, ")
                 SQL.ADD("TablaFeriados1, ")
                 SQL.ADD("Operador, ")
                 SQL.ADD("OpSIOPEL, ")
                
                 SQL.ADD("EnteCus1, ")
                 SQL.ADD("EnteCus2, ")
                 SQL.ADD("Vehiculo1, ")
                 SQL.ADD("Mercado4, ")
                 
                 SQL.ADD("CB5, ")
                 SQL.ADD("NroAltaMAE, ")
                 SQL.ADD("UDN2, ")
                 SQL.ADD("PaseCB, ")
                 
                 SQL.ADD("AceptaMAE, ")
                 SQL.ADD("FechaAltaMAE, ")
                 SQL.ADD("FechaEnvioMAE, ")
                 SQL.ADD("Cuenta1, ")
                 
                 SQL.ADD("Cuenta4, ")
                 SQL.ADD("Cuenta5, ")
                 SQL.ADD("Observaciones, ")
                 SQL.ADD("Numerador, ")
                
                 SQL.ADD("FechaCarga, ")
                 SQL.ADD("FechaCargaSys, ")
                 SQL.ADD("UsrInput, ")
                 SQL.ADD("CB4, ")
                 
                 SQL.ADD("Direccion, ")
                 SQL.ADD("NrOrden, ")
                 SQL.ADD("Corporate, ")
                 SQL.ADD("NrOrden1, ")
                 SQL.ADD("Book1, ")
                 SQL.ADD("MB2, ")
                 SQL.ADD("NrBoleto, ")                 
                 SQL.ADD("AfectaLS ")
                 
                 SQL.ADD(") values (")
               
                 * NrOperacion
                 SQL.ADD(" '"~Val(w6)~"', ")
                 * TipoOp
                 SQL.ADD(" '"~Val(t:2)~"', ")
                 * Especie
                 SQL.ADD(" '"~Val(t:3)~"', ")
                 * Cliente1
                 SQL.ADD(" '"~Val(t:56)~"', ")
                                 
                 * Cantidad
                 SQL.ADD(" "~FStr(Val(t:11),0,2)~", ")
                 * ContraEspecie
                 SQL.ADD(" '"~Val(t:12)~"', ")
                 * Precio1
                 SQL.ADD(" "~FStr(Val(t:13),0,8)~", ")
                 * Cupon1
                 SQL.ADD(" '"~Val(t:17)~"', ")
                 * FechaOp
                 SQL.ADD(" '"~IdxDate(Val(t:19))~"', ")
               
                 * Plazo (N)
                 SQL.ADD(" "~FStr(Val(t:21),0,0)~", ")
                 * RB5 (N)
                 SQL.ADD(" '"~Val(t:20)~"', ")
                 * RB6 (N) Fuente=0=Siopel
                 SQL.ADD(" '0', ")
                 * FechaVto
                 SQL.ADD(" '"~IdxDate(Val(t:45))~"', ")
                 * Mercado
                 SQL.ADD(" '"~Val(t:22)~"', ")
                 * Mercado2
                 SQL.ADD(" '"~Val(t:52)~"', ")
                 * FormaLiquidacion1
                 SQL.ADD(" '"~FStr(Val(t:23),1,0)~"', ")
               
                 * FormaLiquidacion2
                 SQL.ADD(" '"~FStr(Val(t:23),1,0)~"', ")
                 * TablaFeriados1
                 SQL.ADD(" '"~Val(t:24)~"', ")
                 * Operador
                 SQL.ADD(" '"~Val(t:25)~"', ")
                 * OpSIOPEL
                 SQL.ADD(" '"~Val(t:26)~"', ")
              
                 * EnteCus1 (N)
                 SQL.ADD(" '"~Val(t:27)~"', ")
                 * EnteCus2 (N)
                 SQL.ADD(" '"~Val(t:57)~"', ")
                 * Vehiculo1
                 SQL.ADD(" '"~Val(t:29)~"', ")
                 * Mercado4
                 SQL.ADD(" '"~Val(t:31)~"', ")
                
                 * CB5 (Es de Mercado Externo )
                 SQL.ADD(" '1', ")
                 * NroAltaMAE
                 SQL.ADD(" '"~fbn('Boleta')~"', ")
                 * UDN2 (Rueda)
                 SQL.ADD(" '"~Val(t:32)~"', ")
                 * PaseCB
                 SQL.ADD(" '"~Val(t:33)~"', ")
                
                 * AceptaMAE
                 SQL.ADD(" 'SI', ")
                 * FechaAltaMAE
                 SQL.ADD("Convert(datetime,'"~Val(t:84)~"',103), ")
                * FechaEnvioMAE
                 SQL.ADD(" "~Val(t:49)~", ")
                 * Cuenta1
                 SQL.ADD(" '"~Val(t:28)~"', ")

                 * Cuenta4
                 SQL.ADD(" '"~Val(t:80)~"', ")
                 * Cuenta5
                 SQL.ADD(" '"~Val(t:81)~"', ")
                 * Observaciones
                 SQL.ADD(" '"~Val(t:40)~"', ")
                 * Numerador
                 SQL.ADD(" 1, ")
               
                 * FechaCarga
                 SQL.ADD(" "~SQLAhora~", ")
                 * FechaCargaSys
                 SQL.ADD(" "~IdxDate2(FSys)~", ")
                 * UsrInput
                 SQL.ADD(" '"~UsuarioActivo~"', ")
                 * Marca CPC1 x Terceros
                 SQL.ADD(" '"~Val(t:73)~"', ")

                 * Direccion
                 SQL.ADD(" 2, ")
                 * NrOrden
                 SQL.ADD(" '"~Val(t:50)~"', ")
                 * Corporate (Cartera Propia)
                 SQL.ADD(" '"~FStr(Val(t:55),1,0)~"', ")
                 * NrOrden1 (OTIC/OTIV)
                 SQL.ADD(" '"~Val(t:53)~"', ")
                 * Book1
                 SQL.ADD(" '"~Val(t:75)~"', ")
                 * MB2 Bloqueo Moneda
                 SQL.ADD(" '"~Val(t:82)~"', ") 
                 * NrBoleto                
                 SQL.ADD(" '"~PAD0(Val(NR:2),8)~"', ")                 
                 * AfectaLS
                 SQL.ADD(" '"~Val(t:71)~"' ")
                 
                 SQL.ADD(")")
                 SQL.NEW

                 ** generar libro de ordenes
                 SQL.ADD(" Insert into "~DBO~".LIBORDENES ( ")
                 SQL.ADD(" Mercado, Fecha,  FechaOP, NrOperacionRef, FechaHo,  HoraOp, SecuenciaHO, Agente  ")
                 SQL.ADD(" ) Values ( ")
                 SQL.ADD(" '1', ")
                 SQL.ADD(" "~IdxDate2(FSys)~", ")
                 SQL.ADD(" "~IdxDate2(Val(t:19))~", ")
                 SQL.ADD(" '"~Val(w6)~"', ")
                 SQL.ADD(" "~IdxDate2(val(t:19))~", ")
                 SQL.ADD(" '"~Val(t:72)~"', ")
                 SQL.ADD(" '"~fbn('Boleta')~"', ")
                 SQL.ADD(" '"~VAR('AGESIOP')~"' ")
                 SQL.ADD(" ) ")
                 SQL.NEW
 
                 ** Generar Ejecuciones        
                 ACN(NE:1,SQL(" Select IsNull(Max(NrEjecucion),0)+1 From "~DBO~".EJECUCIONES Where NrOrden='"~Val(t:53)~"' "))
                 ACT(w:1, Val(fe:1)~' '~Copy(val(ho:1),1,2)~':'~Copy(val(ho:1),3,2)~':'~Copy(val(ho:1),5,2))
                 **Convert(datetime,'"~Val(w:1)~"',120)
                 SQL.ADD(" Insert Into "~DBO~".EJECUCIONES (NrOrden, NrOperacion, NrEjecucion, NrSecuencia, Fecha, Cantidad, Precio, Cantidad2) ")
                 SQL.ADD(" Values ('"~Val(t:53)~"', '"~Val(w6)~"', '"~FStr(Val(NE:1),0,0)~"', '"~Val(u:1)~"', Convert(datetime,'"~Val(w:1)~"',103), '"~FStr(Val(t:11),0,2)~"', '"~FStr(Val(t:13),0,8)~"', '"~FStr(Val(t:11)*Val(t:13),0,2)~"') ")
                 SQL.NEW
                  
                 SQL.ADD(" Update "~DBO~".ORDENES Set ")
                 SQL.ADD("  NrOperacion2 = '"~Val(w6)~"', ")
                 SQL.ADD("  Cuotas1      = "~FStr(Val(NE:1),0,0)~", ")
                 SQL.ADD("  Cantidad1    = "~FStr(Val(t:11),0,2)~", ")
                 SQL.ADD("  Cantidad4    = "~FStr(Val(t:13),0,8)~" ")
                 SQL.ADD(" Where NrOrden = '"~Val(t:53)~"' ")
                 SQL.NEW
                 SQL.EXEC
                 
                 Message('Modificar Operacion '~Val(w6))
                 ModificarOperacion(Val(w6),0,SI,SI,SI,NO,SI,NO,NO,NO,NO,NO,NO,NO,NO,NO,NO,SI,NO,NO,NO,SI,NO,SI,NO,0,NO,NO)
                 
                 *Recalcula campos y Movimientos instancia 0 por recalculo comisiones
                 ModificarOrden(Val(t:53),0,NO,SI,SI,NO,NO,NO,NO,NO,NO,NO,NO,NO,NO,NO,NO,NO,NO,NO,NO,SI,NO,NO,NO)
                 
                 IF Ok
                    ** Actualizo Direccion para que aparezca en verde en la instancia que corresponda **
                    SQL.ADD("Update "~DBO~".OPERACIONES Set Direccion = 2 where NrOperacion = '"~Val(w6)~"' ")
                    SQL.NEW
                    SQL.EXEC                   
                 Endif
                 ** Instancia Orden 9 Ejecutada  
                 SQL.ADD("Update "~DBO~".OPERACIONESBITS  ")
                 SQL.ADD("Set Valor='0'  ")
                 SQL.ADD(" where NrOperacion = '"~Val(t:53)~"' and Valor = '1' ")
                 SQL.NEW
                 SQL.EXEC           
                 SQL.ADD("Update "~DBO~".OPERACIONESBITS  ")
                 SQL.ADD("Set Valor='1'  ")
                 SQL.ADD(" where NrOperacion = '"~Val(t:53)~"' and Nrbit = 9 ")
                 SQL.NEW
                 SQL.ADD("Update "~DBO~".OPERACIONESBITS  ")
                 SQL.ADD("Set Valor='0'  ")
                 SQL.ADD(" where NrOperacion = '"~Val(w6)~"' and Valor = '1' ")
                 SQL.NEW
                 SQL.EXEC  

                 SQL.ADD("Update "~DBO~".OPERACIONESBITS  ")
                 SQL.ADD("Set Valor='1'  ")
                 SQL.ADD(" where NrOperacion = '"~Val(w6)~"' and Nrbit = 13 ")
                 SQL.NEW
                 SQL.EXEC  
    
                 ** actualiza OPA3
                 SQL.ADD("Update "~DBO~".OPA3 ")
                 SQL.ADD("   Set Procesado   = '1',")
                 SQL.ADD("       Warning       = '"~Trim(Copy(Val(WA:1),1,255))~"', ")
                 SQL.ADD("       NrOperacion = '"~Val(w7)~"' ")
                 SQL.ADD(" where Boleta  = '"~fbn('Boleta')~"' ")
                 SQL.NEW
                 SQL.EXEC
