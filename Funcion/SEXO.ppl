* Esta funcion recibe un codigo de sexo de BANTOTAL y 
* devuelve el equivalente en FPA, si no existe devuelve vacio

IF EqStr(Val(ba0),'M')
   ACT(bb0, 'MAS')
ELSE
   IF EqStr(Val(ba0),'F')
      ACT(bb0, 'FEM')
   ELSE
      ACT(bb0, '')
   ENDIF
ENDIF
