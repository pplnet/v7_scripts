SetFilaActual(Val(fi1))

ACT(a:FAC, 'INVERSIONES')
ACN(fi20, FAC)
(.'COLUMINF2'.)
IFA
AgregarMarco(a:Val(fi1)..a:FAC,2)
AlinearCeldas(a:Val(fi1), 'C')
IFA
ACT(a:FAC, 'Depositos a Plazo')
ACN(fi2, FAC)
IFA
ACT(a:FAC, 'Overnights/Time Deposits')
IFA
ACT(a:FAC, 'Colocaciones en el Exterior en Otras Monedas')
IFA
ACT(a:FAC, 'Fondos Comunes de Inversion')
IFA
ACT(a:FAC, 'Cuentas Corrientes Remuneradas')
IFA
ACT(a:FAC, 'Cajas de Ahorro')
IFA
ACT(a:FAC, 'Colocaciones Vinculadas')
IFA
ACT(a:FAC, 'Operaciones de Cambio a Termino')
IFA
ACT(a:FAC, 'Titulos Comprados a Termino')

IFA
ACT(a:FAC, 'Saldo Otras Cuentas')
ACN(aj1,fac)

IFA
ACT(a:FAC, 'Activos en Transito por Op de Bonos y/o Futuros') 
ACN(aj2,fac)

IFA
ACT(a:FAC, 'Otros Conceptos') 
ACN(aj20,fac)

IFA
ACT(a:FAC, 'USD Atesoramiento') 
ACN(aj21,fac)

IFA
ACT(a:FAC, 'Collateral Operaciones')
IFA
ACN(fi18, FAC)

SetFilaActual(FAC+5)

ACT(a:FAC, 'Cartera de Titulos')
IFA
ACN(fi3, FAC)

SetFilaActual(FAC+32)

ACT(a:FAC, 'Cartera de Titulos Administrada')
IFA
ACN(fi19, FAC)

SetFilaActual(FAC+5)
ACN(fi30 , FAC)
ACT(a:FAC, 'Otras Op.Dinero en Transito Canc.Exterior')
ACN(aj30,fac)
IFA
ACT(a:FAC, 'Pasivos por Operaciones de Bonos y/o Futuros')
ACN(aj31,fac)
IFA
ACT(a:FAC, 'Certificados de Credito Fiscal')
ACN(aj32,fac)


IFA
IFA
ACT(a:FAC, 'Total Activo')
AgregarMarco(a:Val(fi1)..a:FAC,2,0,Exe('COLOR6'))
AgregarMarco(a:FAC..q:FAC,2,0,Exe('COLOR6'))
SetFont(a:FAC, 2)
ACN(fi4, FAC)
IFA
IFA

SetFilaActual(80)
***---Si se cambia el numero de esta fila cambiarlo tambien en la variable FP:1 del informe COSPLA.
***---Si se cambia el orden de los titulos de PASIVOS, cambiarlos en COSPLA.
ACN(fi5, FAC)
ACT(a:FAC, 'PASIVOS')
ACN(fi20, FAC)
(.'COLUMINF2'.)
IFA
AgregarMarco(a:Val(fi5)..a:FAC,2)
AlinearCeldas(a:Val(fi5), 'C')
IFA
ACT(a:FAC, 'Prestamos Terceros Locales')
ACN(fi6, FAC)
IFA
ACT(a:FAC, 'Prestamos Terceros Off-Shore')
IFA
ACT(a:FAC, 'Financiacion de Importaciones (Buyer Credits)')
IFA
ACT(a:FAC, 'Prestamos Sindicados')
IFA
ACT(a:FAC, 'Tomas Vinculadas')
IFA
ACT(a:FAC, 'Operaciones de Cambio a Termino')
IFA 
ACT(a:FAC, 'Titulos Vendidos a Termino')
IFA
ACT(a:FAC, 'Impuesto al Endeudamiento a Pagar')
ACN(aj3,fac)
IFA
ACT(a:FAC, 'WHT a Pagar')
ACN(aj4,fac)
IFA
ACT(a:FAC, 'Otros Conceptos')
ACN(aj22,fac)
IFA
ACT(a:FAC, 'Deudas por Unwind de Derivados')
ACN(aj40,fac)
IFA
ACT(a:FAC, 'Cartas de Credito')
ACN(aj41,fac)
IFA

ACT(a:FAC, 'O.N.s')
IFA
ACN(fi7, FAC)

SetFilaActual(FAC+35)

IFA
IFA
ACT(a:FAC, 'Total Pasivo')
AgregarMarco(a:Val(fi5)..a:FAC,2,0,Exe('COLOR6'))
AgregarMarco(a:FAC..q:FAC,2,0,Exe('COLOR6'))
SetFont(a:FAC, 2)

ACN(fi8, FAC)
IFA
IFA

SetFont(a:Val(fi1)..a:FAC, 2)

ACT(a:FAC, 'Posicion Neta: ')
SetFont(a:FAC..q:FAC, 2)
AgregarMarco(a:FAC..q:FAC,2,0,Exe('COLOR8'))
ACN(fi9, FAC)
IFA
IFA

ACT(a:FAC, 'OTROS CONCEPTOS')
ACN(fi20, FAC)

(.'COLUMINF2'.)
IFA
AgregarMarco(a:Val(fi20)..a:FAC,2,0,Exe('COLOR6'))
AlinearCeldas(a:Val(fi20), 'C')
IFA
ACT(a:FAC, 'Diferimiento Treasury Lock')
ACN(aj6, FAC)
IFA
ACT(a:FAC, "Underwriting Fee/Honorarios ON's/Buyers/Sindicado ")
ACN(aj7, FAC)
IFA
ACT(a:FAC, "Otros Fees a Devengar")
ACN(aj8, FAC)

SetFont(a:Val(fi1)..a:FAC, 2)

IFA
IFA
ACT(a:FAC, 'Total Otros Conceptos')
AgregarMarco(a:Val(fi5)..a:FAC,2,0,Exe('COLOR6'))
AgregarMarco(a:FAC..q:FAC,2,0,Exe('COLOR6'))
ACN(aj5, FAC)
SetFont(a:FAC, 2)

IFA
IFA

ACT(a:FAC, 'Posicion Contable: ')
SetFont(a:FAC..q:FAC, 2)
AgregarMarco(a:FAC..q:FAC,2,0,Exe('COLOR8'))
ACN(aj9, FAC)
IFA
IFA

ACO(1, 27) 

