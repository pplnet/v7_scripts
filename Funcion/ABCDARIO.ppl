CrearFont(1,"Arial,8,B")

ACN(a3, 0)
Recorrer&i 0, 26
   IF &i = 0
      ACT(a1, '')
   ENDIF
   IF &i = 1
      ACT(a1, 'A')
   ENDIF
   IF &i = 2
      ACT(a1, 'B')
   ENDIF
   IF &i = 3
      ACT(a1, 'C')
   ENDIF
   IF &i = 4
      ACT(a1, 'D')
   ENDIF
   IF &i = 5
      ACT(a1, 'E')
   ENDIF
   IF &i = 6
      ACT(a1, 'F')
   ENDIF
   IF &i = 7
      ACT(a1, 'G')
   ENDIF
   IF &i = 8
      ACT(a1, 'H')
   ENDIF
   IF &i = 9
      ACT(a1, 'I')
   ENDIF
   IF &i = 10
      ACT(a1, 'J')
   ENDIF
   IF &i = 11
      ACT(a1, 'K')
   ENDIF
   IF &i = 12
      ACT(a1, 'L')
   ENDIF
   IF &i = 13
      ACT(a1, 'M')
   ENDIF
   IF &i = 14
      ACT(a1, 'N')
   ENDIF
   IF &i = 15
      ACT(a1, 'O')
   ENDIF
   IF &i = 16
      ACT(a1, 'P')
   ENDIF
   IF &i = 17
      ACT(a1, 'Q')
   ENDIF
   IF &i = 18
      ACT(a1, 'R')
   ENDIF
   IF &i = 19
      ACT(a1, 'S')
   ENDIF
   IF &i = 20
      ACT(a1, 'T')
   ENDIF
   IF &i = 21
      ACT(a1, 'U')
   ENDIF
   IF &i = 22
      ACT(a1, 'V')
   ENDIF
   IF &i = 23
      ACT(a1, 'W')
   ENDIF
   IF &i = 24
      ACT(a1, 'X')
   ENDIF
   IF &i = 25
      ACT(a1, 'Y')
   ENDIF
   IF &i = 26
      ACT(a1, 'Z')
   ENDIF

   Recorrer&j 1, 26
      IF &j = 1
         ACT(a2, 'A')
      ENDIF
      IF &j = 2
         ACT(a2, 'B')
      ENDIF
      IF &j = 3
         ACT(a2, 'C')
      ENDIF
      IF &j = 4
         ACT(a2, 'D')
      ENDIF
      IF &j = 5
         ACT(a2, 'E')
      ENDIF
      IF &j = 6
         ACT(a2, 'F')
      ENDIF
      IF &j = 7
         ACT(a2, 'G')
      ENDIF
      IF &j = 8
         ACT(a2, 'H')
      ENDIF
      IF &j = 9
         ACT(a2, 'I')
      ENDIF
      IF &j = 10
         ACT(a2, 'J')
      ENDIF
      IF &j = 11
         ACT(a2, 'K')
      ENDIF
      IF &j = 12
         ACT(a2, 'L')
      ENDIF
      IF &j = 13
         ACT(a2, 'M')
      ENDIF
      IF &j = 14
         ACT(a2, 'N')
      ENDIF
      IF &j = 15
         ACT(a2, 'O')
      ENDIF
      IF &j = 16
         ACT(a2, 'P')
      ENDIF
      IF &j = 17
         ACT(a2, 'Q')
      ENDIF
      IF &j = 18
         ACT(a2, 'R')
      ENDIF
      IF &j = 19
         ACT(a2, 'S')
      ENDIF
      IF &j = 20
         ACT(a2, 'T')
      ENDIF
      IF &j = 21
         ACT(a2, 'U')
      ENDIF
      IF &j = 22
         ACT(a2, 'V')
      ENDIF
      IF &j = 23
         ACT(a2, 'W')
      ENDIF
      IF &j = 24
         ACT(a2, 'X')
      ENDIF
      IF &j = 25
         ACT(a2, 'Y')
      ENDIF
      IF &j = 26
         ACT(a2, 'Z')
      ENDIF
           
      SCN(a3, 1)
      ACT(Val(a3):0, Val(a1)~Val(a2))
      SetFont(Val(a3):0, 1)
      AlinearCeldas(Val(a3):0,'C')
      ACT(a2, '')
   Proximo
   ACT(a1, '')
Proximo
ACT(a3, '')
