***** Ingreso Transaccion2 'MATCH'
      if Dialogo.Check8=1
      * Procesa SI
        ACT(n:&i,GetNumerador(12)) 
        SQL.ADD("Insert into "~DBO~".TRANSACCIONES2 ( ")
             
        SQL.ADD("TipoTr2,  ")
        SQL.ADD("Especie,  ")
        SQL.ADD("Fecha1,   ")
        SQL.ADD("Fecha2,   ")
        SQL.ADD("Cliente1, ")
             
        SQL.ADD("NrOperacionRef1, ")
        SQL.ADD("Cantidad1, ")
        SQL.ADD("Especie1,  ")
        SQL.ADD("Cantidad2, ")
        SQL.ADD("Mercado1,  ")
             
        SQL.ADD("Vehiculo1, ")

        * Especie Cliente 
        SQL.ADD("Cuenta1,   ")
        * Especie Propia 
        SQL.ADD("Cuenta2,   ")
        * Moneda Cliente 
        SQL.ADD("Cuenta3,   ")
        * Moneda Propia  
        SQL.ADD("Cuenta4,   ")
             
        SQL.ADD("Cuenta5, ")
        SQL.ADD("Cuenta6, ")
        SQL.ADD("Cuenta7, ")
        SQL.ADD("Cuenta8, ")
        SQL.ADD("Observaciones, ")
             
        SQL.ADD("Cupon1, ")
        SQL.ADD("NrTrans, ")

        if DesciendeDeEspecie(Val(j:&i),'TITULO') o DesciendeDeEspecie(Val(j:&i),'CUPON') o DesciendeDeEspecie(Val(bp:&i),'TITULO') o DesciendeDeEspecie(Val(bp:&i),'CUPON')
        SQL.ADD("NrOperacionRef4, ")
        endif

        SQL.ADD(" Cantidad11, ")
        SQL.ADD(" Cantidad25, ")
        SQL.ADD(" Cantidad12, ")
        SQL.ADD(" Cantidad26, ")
        SQL.ADD(" Especie3,   ")

        SQL.ADD(" FechaCarga, FechaCargaSys, UsrInput, Book1 ")
             
        SQL.ADD(" ) Values ( ")
             
        SQL.ADD(" 'MATCH', ")
        SQL.ADD(" '"~Val(j:&i)~"', ")
        SQL.ADD(" '"~IdxDate(FSys)~"', ")
        SQL.ADD(" '"~IdxDate(Dialogo.Fecha1)~"', ")
        SQL.ADD(" '"~Val(a:&i)~"', ")
             
        SQL.ADD(" '"~Val(u:&i)~"', ")
        SQL.ADD(" "~FStr(Val(k:&i),1,2)~", ")
        SQL.ADD(" '"~Val(bp:&i)~"', ")
        SQL.ADD(" "~FStr(Val(bq:&i),1,2)~", ")
        SQL.ADD(" '"~Val(b:&i)~"', ")
             
        SQL.ADD(" '"~Dialogo.Vehiculo1~"', ")
        SQL.ADD(" '"~Val(r:&i)~"', ")
        SQL.ADD(" '"~Val(s:&i)~"', ")
*FO20/02/2006 Las cuentas de moneda son las mismas de titulos para vouchers match
*        SQL.ADD(" '"~Val(br:&i)~"', ")
*        SQL.ADD(" '"~Val(71:&i)~"', ")
        SQL.ADD(" '"~Val(r:&i)~"', ")
        SQL.ADD(" '"~Val(s:&i)~"', ")

             
        SQL.ADD(" '"~Val(r:&i)~"', ")
        SQL.ADD(" '"~Val(s:&i)~"', ")
*FO20/02/2006 Las cuentas de moneda son las mismas de titulos para vouchers match
*        SQL.ADD(" '"~Val(br:&i)~"', ")
*        SQL.ADD(" '"~Val(71:&i)~"', ")
        SQL.ADD(" '"~Val(r:&i)~"', ")
        SQL.ADD(" '"~Val(s:&i)~"', ")

        ACT(cz1, 'Cta.Cliente Especie: ')
        if NoVacio(Val(r:&i))
          ACT(cz1, Val(cz1)~'Depos. ')
          ACT(cy1, BuscarCampo('Cuentas','Comitente','Codigo',Val(r:&i)))
          if NoVacio(Val(cy1))
             ACT(cz1, Val(cz1)~BuscarCampo('Clientes','Alias2','Codigo',Val(r:&i)))
          else
             ACT(cz1, Val(cz1)~BuscarCampo('Cuentas','NrDepositante','Codigo',Val(cy1)))
          endif
          ACT(cz1, Val(cz1)~' Comit. '~BuscarCampo('Cuentas','Numero','Codigo',Val(r:&i))~' '~BuscarCampo('Cuentas','Alias3','Codigo',Val(r:&i)))
        endif
        ACT(cz2, 'Cta.Cliente Moneda: ')
*FO20/02/2006        if NoVacio(Val(br:&i))
        if NoVacio(Val(r:&i))
           ACT(cz2, Val(cz2)~'Depos. ')
           ACT(cy2,BuscarCampo('Cuentas','Comitente','Codigo',Val(o:&i)))
           if NoVacio(Val(cy2))
              ACT(cz2, Val(cz2)~BuscarCampo('Clientes','Alias2','Codigo',Val(o:&i)))
           else
              ACT(cz2, Val(cz2)~BuscarCampo('Cuentas','NrDepositante','Codigo',Val(cy2)))
           endif
           ACT(cz2, Val(cz2)~' Comit. '~BuscarCampo('Cuentas','Numero','Codigo',Val(o:&i))~' '~BuscarCampo('Cuentas','Alias3','Codigo',Val(r:&i)))
        endif

        SQL.ADD(" '"~Val(cz1)~CHR(13)~CHR(10)~Val(cz2)~CHR(13)~CHR(10)~"', ")

        if DesciendeDeEspecie(Val(j:&i),'MONEDA') 
           SQL.ADD(" null, ")
        else
           SQL.ADD(" '"~Val(t:&i)~"', ")
        Endif
        SQL.ADD(" '"~Val(n:&i)~"', ")

        if DesciendeDeEspecie(Val(j:&i),'TITULO') o DesciendeDeEspecie(Val(j:&i),'CUPON') o DesciendeDeEspecie(Val(bp:&i),'TITULO') o DesciendeDeEspecie(Val(bp:&i),'CUPON')
           SQL.ADD(" '"~Val(nu1)~"',  ")
        endif
*------
        * No se alimenta el valor aun
        SQL.ADD(""~FStr(Val(dk:&i),1,2)~", ")
        * Es por limites Fijo VER
** FJL 16/10/22 - Para desbloqueqar TIDE
*        SQL.ADD(" '2', ")
        SQL.ADD(""~FStr(Val(dj:&i),1,0)~", ")
        SQL.ADD(FSTR(SQL("SELECT PlazoParking From "~DBO~".ESPECIES WHERE Codigo='"~BuscarCampo('OPERACIONES','ContraEspecie','NrOperacion',Val(i:FAC))~"' "),1,0)~",")

        * Es por la operacion
        SQL.ADD("'0', ")
        SQL.ADD(" '"~BuscarCampo('OPERACIONES','ContraEspecie','NrOperacion',Val(i:FAC))~"',  ")
            
*------          

        SQL.ADD(" "~SQLAhora~", "~IdxDate2(FSys)~", '"~UsuarioActivo~"', '"~BuscarCampo('OPERACIONES','Book1','NrOperacion',Val(i:FAC))~"' ")
        SQL.ADD(")")
        SQL.NEW
             
        SQL.ADD("Insert into "~DBO~".OPERACIONESBITS ")
        SQL.ADD("(NrBit, NrOperacion, Valor) ")
        SQL.ADD("Select NrInstancia, '"~Val(n:&i)~"', '0'")
        SQL.ADD("from "~DBO~".INSTANCIAS ")
        SQL.ADD("where Tabla = 'TRANSACCIONES2'")
        SQL.NEW
             
        SQL.ADD(" UPDATE "~DBO~".OPERACIONESBITS ")
        ** Ana cambio esta linea por el de abajo *** 
        SQL.ADD(" SET Valor = '1' ")
*        if Val(k:&i) <= 0 
*           ** Es un cobro
*           SQL.ADD(" Where NrBit = 3 and NrOperacion = '"~Val(n:&i)~"' ")
*        else
*           ** Es un pago
*           SQL.ADD(" Where NrBit = 6 and NrOperacion = '"~Val(n:&i)~"' ")
*        endif
        SQL.ADD(" Where NrBit = 14 and NrOperacion = '"~Val(n:&i)~"' ")
        SQL.NEW
             
Message('Genera Voucher '~Val(n:&i))
             
***** Movs de la Transaccion2
** Especie (Titulo)            
        if Dialogo.Fecha1>FSys
          SQL.ADD("Insert into "~DBO~".MOVPENDIENTES ( ")
        else
          SQL.ADD("Insert into "~DBO~".MOVEJECUTADOS ( ")
          SQL.ADD("FechaEje, ")
          SQL.ADD("UsuarioEje, ")
        endif
             
        SQL.ADD("NrTrans2, ")
        SQL.ADD("NrOperacion, ")
        SQL.ADD("Leyenda, ")
        SQL.ADD("Cantidad, ")
        SQL.ADD("Cliente, ")
        SQL.ADD("Especie, ")
             
        SQL.ADD("CallPut, ")
        SQL.ADD("NrSaldo, ")
        SQL.ADD("NrGrupo, ")
        SQL.ADD("NrGrupo2, ")
        SQL.ADD("Bits, ")
             
        SQL.ADD("Cupon, ")
        SQL.ADD("NrMov, ")
        SQL.ADD("FechaMov, ")
        SQL.ADD("EnteCustodia, ")
        SQL.ADD("Mercado, ")
             
        SQL.ADD("Concepto, ")
        SQL.ADD("Cuenta")
             
        SQL.ADD( ") Values ( ")
        if Dialogo.Fecha1<=FSys
          SQL.ADD(" '"~IdxDate(FSys)~"', ")
          SQL.ADD(" '"~UsuarioActivo~"', ")
        endif
             
        SQL.ADD(" '"~Val(n:&i)~"', ")
        SQL.ADD(" '"~Val(n:&i)~"', ")
        SQL.ADD(" 'Match', ")
        SQL.ADD(" "~FStr(-Val(k:&i),1,2)~", ")
        SQL.ADD(" '"~Dialogo.Vehiculo1~"', ")
        SQL.ADD(" '"~Val(j:&i)~"', ")
             
        SQL.ADD(" '', ")
        SQL.ADD(" "~FStr(1,1,0)~", ")
        SQL.ADD(" "~FStr(0,1,0)~", ")
        SQL.ADD(" "~FStr(0,1,0)~", ")
        SQL.ADD(" 'AV', ")
         
        if DesciendeDeEspecie(Val(j:&i),'MONEDA') 
           SQL.ADD(" null,  ")
        else     
           SQL.ADD(" '"~Val(t:&i)~"', ")
        endif

        SQL.ADD(" "~FStr(0,1,0)~", ")
        SQL.ADD(" '"~IdxDate(Dialogo.Fecha1)~"', ")
        SQL.ADD(" '"~Dialogo.Vehiculo1~"', ")
        SQL.ADD(" '"~Val(b:&i)~"', ")
             
        SQL.ADD(" '"~Val(c:&i)~"', ")
        SQL.ADD(" '"~Val(s:&i)~"'")
             
        SQL.ADD(" ) ")
        SQL.NEW
             
        if EqStr(BuscarCampo('Cuentas','Comitente','Codigo',Val(r:&i)),Dialogo.Vehiculo1)
          if Dialogo.Fecha1>FSys
            SQL.ADD("Insert into "~DBO~".MOVPENDIENTES ( ")
          else
            SQL.ADD("Insert into "~DBO~".MOVEJECUTADOS ( ")
            SQL.ADD("FechaEje, ")
            SQL.ADD("UsuarioEje, ")
          endif
             
          SQL.ADD("NrTrans2, ")
          SQL.ADD("NrOperacion, ")
          SQL.ADD("Leyenda, ")
          SQL.ADD("Cantidad, ")
          SQL.ADD("Cliente, ")
          SQL.ADD("Especie, ")
             
          SQL.ADD("CallPut, ")
          SQL.ADD("NrSaldo, ")
          SQL.ADD("NrGrupo, ")
          SQL.ADD("NrGrupo2, ")
          SQL.ADD("Bits, ")
             
          SQL.ADD("Cupon, ")
          SQL.ADD("NrMov, ")
          SQL.ADD("FechaMov, ")
          SQL.ADD("EnteCustodia, ")
          SQL.ADD("Mercado, ")
             
          SQL.ADD("Concepto, ")
          SQL.ADD("Cuenta ")
             
          SQL.ADD(" ) Values ( ")
          if Dialogo.Fecha1<=FSys
            SQL.ADD(" '"~IdxDate(FSys)~"', ")
            SQL.ADD(" '"~UsuarioActivo~"', ")
          endif
             
          SQL.ADD(" '"~Val(n:&i)~"', ")
          SQL.ADD(" '"~Val(n:&i)~"', ")
          SQL.ADD(" 'Match', ")
          SQL.ADD(" "~FStr(-Val(k:&i),1,2)~", ")
          SQL.ADD(" '"~Val(a:&i)~"', ")
          SQL.ADD(" '"~Val(j:&i)~"', ")
             
          SQL.ADD(" '', ")
          SQL.ADD(" "~FStr(1,1,0)~", ")
          SQL.ADD(" "~FStr(0,1,0)~", ")
          SQL.ADD(" "~FStr(0,1,0)~", ")
          SQL.ADD(" 'AV', ")

          if DesciendeDeEspecie(Val(j:&i),'MONEDA') 
             SQL.ADD(" null, ")
          else
             SQL.ADD(" '"~Val(t:&i)~"', ")             
          endif

          SQL.ADD(" "~FStr(0,1,0)~", ")
          SQL.ADD(" '"~IdxDate(Dialogo.Fecha1)~"', ")
          SQL.ADD(" '"~Dialogo.Vehiculo1~"', ")
          SQL.ADD(" '"~Val(b:&i)~"', ")
           
          SQL.ADD(" '"~Val(c:&i)~"', ")
          SQL.ADD(" '"~Val(r:&i)~"' ")
             
          SQL.ADD(" ) ")
          SQL.NEW             
        endif
             
** ContraEspecie (Moneda)            
        if Dialogo.Fecha1>FSys
          SQL.ADD("Insert into "~DBO~".MOVPENDIENTES ( ")
        else
          SQL.ADD("Insert into "~DBO~".MOVEJECUTADOS ( ")
          SQL.ADD("FechaEje, ")
          SQL.ADD("UsuarioEje, ")
        endif
             
        SQL.ADD("NrTrans2, ")
        SQL.ADD("NrOperacion, ")
        SQL.ADD("Leyenda, ")
        SQL.ADD("Cantidad, ")
        SQL.ADD("Cliente, ")
        SQL.ADD("Especie, ")
             
        SQL.ADD("CallPut, ")
        SQL.ADD("NrSaldo, ")
        SQL.ADD("NrGrupo, ")
        SQL.ADD("NrGrupo2, ")
        SQL.ADD("Bits, ")
             
        SQL.ADD("NrMov, ")
        SQL.ADD("FechaMov, ")
        SQL.ADD("EnteCustodia, ")
        SQL.ADD("Mercado, ")
             
        SQL.ADD("Concepto, ")
        SQL.ADD("Cuenta")
             
        SQL.ADD(" ) Values ( ")
        if Dialogo.Fecha1<=FSys
          SQL.ADD(" '"~IdxDate(FSys)~"', ")
          SQL.ADD(" '"~UsuarioActivo~"', ")
        endif
             
        SQL.ADD(" '"~Val(n:&i)~"', ")
        SQL.ADD(" '"~Val(n:&i)~"', ")
        SQL.ADD(" 'Match', ")
        SQL.ADD(" "~FStr(-Val(bq:&i),1,2)~", ")
        SQL.ADD(" '"~Dialogo.Vehiculo1~"', ")
        SQL.ADD(" '"~Val(bp:&i)~"', ")
             
        SQL.ADD(" '', ")
        SQL.ADD(" "~FStr(1,1,0)~", ")
        SQL.ADD(" "~FStr(0,1,0)~", ")
        SQL.ADD(" "~FStr(0,1,0)~", ")
        SQL.ADD(" 'AV', ")
             
        SQL.ADD(" "~FStr(0,1,0)~", ")
        SQL.ADD(" '"~IdxDate(Dialogo.Fecha1)~"', ")
        SQL.ADD(" '"~Dialogo.Vehiculo1~"', ")
        SQL.ADD(" '"~Val(b:&i)~"', ")
             
        SQL.ADD(" '"~Val(c:&i)~"', ")
*FO20/02/2006        SQL.ADD(" '"~Val(71:&i)~"' ")
        SQL.ADD(" '"~Val(s:&i)~"' ")
             
        SQL.ADD(" ) ")
        SQL.NEW
             
        if EqStr(BuscarCampo('Cuentas','Comitente','Codigo',Val(r:&i)),Dialogo.Vehiculo1)
          if Dialogo.Fecha1>FSys
            SQL.ADD("Insert into "~DBO~".MOVPENDIENTES (")
          else
            SQL.ADD("Insert into "~DBO~".MOVEJECUTADOS (")
            SQL.ADD("FechaEje, ")
            SQL.ADD("UsuarioEje, ")
          endif
             
          SQL.ADD("NrTrans2, ")
          SQL.ADD("NrOperacion, ")
          SQL.ADD("Leyenda, ")
          SQL.ADD("Cantidad, ")
          SQL.ADD("Cliente, ")
          SQL.ADD("Especie, ")
             
          SQL.ADD("CallPut, ")
          SQL.ADD("NrSaldo, ")
          SQL.ADD("NrGrupo, ")
          SQL.ADD("NrGrupo2, ")
          SQL.ADD("Bits, ")
             
          SQL.ADD("NrMov, ")
          SQL.ADD("FechaMov, ")
          SQL.ADD("EnteCustodia, ")
          SQL.ADD("Mercado, ")
             
          SQL.ADD("Concepto, ")
          SQL.ADD("Cuenta ")
             
          SQL.ADD(" ) Values ( ")
          if Dialogo.Fecha1<=FSys
            SQL.ADD(" '"~IdxDate(FSys)~"', ")
            SQL.ADD(" '"~UsuarioActivo~"', ")
          endif
             
          SQL.ADD(" '"~Val(n:&i)~"', ")
          SQL.ADD(" '"~Val(n:&i)~"', ")
          SQL.ADD(" 'Match', ")
          SQL.ADD(" "~FStr(-Val(bq:&i),1,2)~", ")
          SQL.ADD(" '"~Val(a:&i)~"', ")
          SQL.ADD(" '"~Val(bp:&i)~"', ")
             
          SQL.ADD(" '', ")
          SQL.ADD(" "~FStr(1,1,0)~", ")
          SQL.ADD(" "~FStr(0,1,0)~", ")
          SQL.ADD(" "~FStr(0,1,0)~", ")
          SQL.ADD(" 'AV', ")
             
          SQL.ADD(" "~FStr(0,1,0)~", ")
          SQL.ADD(" '"~IdxDate(Dialogo.Fecha1)~"', ")
          SQL.ADD(" '"~Dialogo.Vehiculo1~"', ")
          SQL.ADD(" '"~Val(b:&i)~"', ")
             
          SQL.ADD(" '"~Val(c:&i)~"', ")
*FO20/02/2006          SQL.ADD(" '"~Val(br:&i)~"' ")
          SQL.ADD(" '"~Val(r:&i)~"' ")
             
          SQL.ADD(" ) ")
          SQL.NEW             
        endif
             
        SQL.ADD("INSERT INTO "~DBO~".MOVEJECUTADOS ")
        SQL.ADD("(NrOperacion, FechaMov, Cliente,  Especie,      CallPut, ")
        SQL.ADD(" FechaEj,     PrecioEj, Mercado,  EnteCustodia, Cuenta,  ")
        SQL.ADD(" Transaccion, Cantidad, Leyenda,  NrSaldo,      NrGrupo, ")
        SQL.ADD(" NrGrupo2,    NrMov,    Concepto, NrInstan,     Status,  ")
        SQL.ADD(" Bits,        NrLiq,    FechaEje, UsuarioEje,   Cupon,   ")
        SQL.ADD(" NrTrans2,    Book     ")
        SQL.ADD(") ")
        SQL.ADD("select ")
        SQL.ADD("NrOperacion, FechaMov, Cliente,            Especie,             CallPut, ")
*FO20/02/2006        SQL.ADD("FechaEj,     PrecioEj, Mercado,            EnteCustodia,        Cuenta,  ")
        SQL.ADD("FechaEj,     PrecioEj, Mercado,            EnteCustodia,        '"~Val(r:&i)~"',  ")
        SQL.ADD("Transaccion, Cantidad, Leyenda,            NrSaldo,             NrGrupo, ")
        SQL.ADD("NrGrupo2,    NrMov,    Concepto,           NrInstan,            Status,  ")
        SQL.ADD("Bits,        NrLiq,    '"~IdxDate(FSys)~"', '"~UsuarioActivo~"', Cupon,   ")
        SQL.ADD("'"~Val(n:&i)~"' ,    Book ")
        SQL.ADD("from "~DBO~".MOVPENDIENTES ")
        SQL.ADD("where NrOperacion = '"~Val(u:&i)~"' ")
        SQL.ADD("and NrSaldo  =  1 ")
        SQL.ADD("and FechaMov <= '"~IdxDate(Dialogo.Fecha1)~"' ")
        SQL.ADD("and Mercado  =  '"~Val(b:&i)~"' ")
        SQL.NEW
                          
        SQL.ADD("DELETE FROM "~DBO~".MOVPENDIENTES ")
        SQL.ADD("where NrOperacion = '"~Val(u:&i)~"' ")
        SQL.ADD("and NrSaldo  =  1 ")
        SQL.ADD("and FechaMov <= '"~IdxDate(Dialogo.Fecha1)~"' ")
        SQL.ADD("and Mercado  =  '"~Val(b:&i)~"' ")
        SQL.NEW
      else
        ACT(n:&i, 'Liquida')
      endif
      * Fin Control Check8=1 (Procesa)

      * ------------------------------------------------ *
      * FO 21/02/2006 - ACTUALIZO LAS CUENTAS DE LAS OPERACIONES
      SQL.ADD(" UPDATE "~DBO~".OPERACIONES ")
      SQL.ADD(" SET Cuenta1 =  '"~Val(r:&i)~"', ")
      SQL.ADD("     Cuenta4 =  '"~Val(r:&i)~"', ")
      SQL.ADD("     Cuenta2 =  '"~Val(s:&i)~"', ")
      SQL.ADD("     Cuenta3 =  '"~Val(s:&i)~"' ")
      SQL.ADD(" WHERE NrOperacion = '"~Val(u:&i)~"'  ")
      SQL.NEW
      SQL.EXEC
      * ------------------------------------------------ *
      