SQL.ADD("Select O.Vehiculo1 As Clave,O.Especie,C.Fecha3 Vto,C.Precio3 Importe,O.NrOperacion,'CUO' as Tipo,O.TipoOp,'WHT' as DescImp,2 as BD1,")
SQL.ADD("CASE  O.TipoOp WHEN 'MMFIMV' THEN 'F.R.N. / YANKEE'  ELSE 'PRESTAMOS SINDICADOS' END Grupo,O.Cliente1,O.FechaOp,O.FechaVto,' ' as MarcaSwap,O.OperacionRef,O.FechaOp2,'' Inicio,'' Fin,O.TipoTasa,C.Precio2 Tasa,C.NrCuota,' ' Pata")
SQL.ADD("from "~DBO~".CUOTAS C,"~DBO~".OPERACIONES O")
SQL.ADD("where O.NrOperacion=C.NrOperacion")
IF NoVacio(Dialogo.Lista1)
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Dialogo.Lista1)~")")
ELSE
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(LS:1000))~")")
ENDIF
IF NoVacio(Dialogo.Lista2)
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Dialogo.Lista2)~")")
ELSE
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:1100))~")")
ENDIF
If NoVacio(Dialogo.Cliente1)
 SQL.ADD("and O.Cliente1='"~Dialogo.Cliente1~"'")
Endif
if NoVacio(Dialogo.Especie1)
 SQL.ADD("and O.Especie='"~Dialogo.Especie1~"'")
endif
SQL.ADD("and O.TipoOp In ('MMFIMV','MMSIMV')")
SQL.ADD("and O.FechaOp<= '"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("and O.RB5='0' and C.Fecha3 >='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("AND C.Fecha3 >='"~IdxDate(Dialogo.Fecha2)~"'")
IF Dialogo.Fecha3 <> 0
 SQL.ADD("AND C.Fecha3<= '"~IdxDate(Dialogo.Fecha3)~"'")
ENDIF
IF Dialogo.Radio5=0
 SQL.ADD("AND C.Precio3 > 0")
ENDIF
SQL.ADD("Union")
SQL.ADD("Select O.Vehiculo1 As Clave,O.Especie,C.Fecha3 Vto,C.Total9 Importe,O.NrOperacion,'CUO' as Tipo,O.TipoOp,'IMP. INTERES' as DescImp,2 as BD1,")
SQL.ADD("CASE O.TipoOp WHEN 'MMFIMV' THEN 'F.R.N. / YANKEE' ELSE 'PRESTAMOS SINDICADOS' END Grupo,")
SQL.ADD("O.Cliente1,O.FechaOp,O.FechaVto,' ' as MarcaSwap,O.OperacionRef,O.FechaOp2,'' Inicio,'' Fin,O.TipoTasa,C.Precio2 Tasa,C.NrCuota,' ' Pata")
SQL.ADD("from "~DBO~".CUOTAS C,"~DBO~".OPERACIONES O")
SQL.ADD("where O.NrOperacion=C.NrOperacion")
IF NoVacio(Dialogo.Lista1)
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Dialogo.Lista1)~")")
ELSE
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(LS:1000))~")")
ENDIF
IF NoVacio(Dialogo.Lista2)
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Dialogo.Lista2)~")")
ELSE
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:1100))~")")
ENDIF
If NoVacio(Dialogo.Cliente1)
 SQL.ADD("and O.Cliente1='"~Dialogo.Cliente1~"'")
Endif
if NoVacio(Dialogo.Especie1)
 SQL.ADD("and O.Especie='"~Dialogo.Especie1~"'")
endif
SQL.ADD("and O.TipoOp In ('MMFIMV','MMSIMV')")
SQL.ADD("and O.FechaOp<= '"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("and O.RB5='0' and C.Fecha3 >='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("AND C.Fecha3 >='"~IdxDate(Dialogo.Fecha2)~"'")
IF Dialogo.Fecha3 <> 0
 SQL.ADD("AND C.Fecha3<= '"~IdxDate(Dialogo.Fecha3)~"'")
ENDIF
IF Dialogo.Radio5=0
 SQL.ADD("AND C.Total9 > 0")
ENDIF
SQL.ADD("Union")
SQL.ADD("Select O.Vehiculo1 As Clave,O.Especie,O.FechaVto Vto,C.Precio3 Importe,O.NrOperacion,'CUO' as Tipo,O.TipoOp,'WHT' as DescImp,2 as BD1,")
SQL.ADD("CASE O.TipoOp WHEN 'MMFIMV' THEN 'F.R.N. / YANKEE' ELSE 'PRESTAMOS SINDICADOS' END Grupo,")
SQL.ADD("O.Cliente1,O.FechaOp,O.FechaVto,' ' as MarcaSwap,O.OperacionRef,O.FechaOp2,'' Inicio,'' Fin,O.TipoTasa,C.Precio2 Tasa,C.NrCuota,' ' Pata")
SQL.ADD("from "~DBO~".CUOTAS C,"~DBO~".OPERACIONES O")
SQL.ADD("where O.TipoOp In ('MMFIMV','MMSIMV')")
SQL.ADD("AND O.NrOperacion=C.NrOperacion")
IF NoVacio(Dialogo.Lista1)
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Dialogo.Lista1)~")")
ELSE
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(LS:1000))~")")
ENDIF
IF NoVacio(Dialogo.Lista2)
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Dialogo.Lista2)~")")
ELSE
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:1100))~")")
ENDIF
If NoVacio(Dialogo.Cliente1)
 SQL.ADD("and O.Cliente1='"~Dialogo.Cliente1~"'")
Endif
if NoVacio(Dialogo.Especie1)
 SQL.ADD("and O.Especie='"~Dialogo.Especie1~"'")
endif
SQL.ADD("and O.FechaOp<= '"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("and O.RB5='1' and O.FechaVto >='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("AND O.FechaVto >='"~IdxDate(Dialogo.Fecha2)~"'")
IF Dialogo.Fecha3 <> 0
 SQL.ADD("AND O.FechaVto<= '"~IdxDate(Dialogo.Fecha3)~"'")
ENDIF
IF Dialogo.Radio5=0
 SQL.ADD("AND C.Precio3 > 0")
ENDIF
SQL.ADD("Union")
SQL.ADD("Select O.Vehiculo1 As Clave,O.Especie,O.FechaVto Vto,C.Total9 Importe,O.NrOperacion,'CUO' as Tipo,O.TipoOp,'IMP. INTERES' as DescImp,2 as BD1,")
SQL.ADD("CASE O.TipoOp WHEN 'MMFIMV' THEN 'F.R.N. / YANKEE' ELSE 'PRESTAMOS SINDICADOS' END Grupo,")
SQL.ADD("O.Cliente1,O.FechaOp,O.FechaVto,' ' as MarcaSwap,O.OperacionRef,O.FechaOp2,'' Inicio,'' Fin,O.TipoTasa,C.Precio2 Tasa,C.NrCuota,' ' Pata")
SQL.ADD("from "~DBO~".CUOTAS C,"~DBO~".OPERACIONES O")
SQL.ADD("where O.TipoOp In ('MMFIMV','MMSIMV')")
SQL.ADD("AND O.NrOperacion=C.NrOperacion")
IF NoVacio(Dialogo.Lista1)
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Dialogo.Lista1)~")")
ELSE
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(LS:1000))~")")
ENDIF
IF NoVacio(Dialogo.Lista2)
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Dialogo.Lista2)~")")
ELSE
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:1100))~")")
ENDIF
If NoVacio(Dialogo.Cliente1)
 SQL.ADD("and O.Cliente1='"~Dialogo.Cliente1~"'")
Endif
if NoVacio(Dialogo.Especie1)
 SQL.ADD("and O.Especie='"~Dialogo.Especie1~"'")
endif
SQL.ADD("and O.FechaOp<= '"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("and O.RB5='1' and O.FechaVto >='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("AND O.FechaVto >='"~IdxDate(Dialogo.Fecha2)~"'")
IF Dialogo.Fecha3 <> 0
 SQL.ADD("AND O.FechaVto<= '"~IdxDate(Dialogo.Fecha3)~"'")
ENDIF
IF Dialogo.Radio5=0
 SQL.ADD("AND C.Total9 > 0")
ENDIF
SQL.ADD("Union")
SQL.ADD("Select O.Vehiculo1 As Clave,O.Especie,C.Fecha3 Vto,C.Total7 Importe,O.NrOperacion,'CUO' as Tipo,O.TipoOp,'WHT' as DescImp,2 as BD1,")
SQL.ADD("CASE O.TipoOp WHEN 'MMPCLM' THEN 'PRESTAMOS FINANCIEROS' WHEN 'MMPEM ' THEN 'BUYERS' WHEN 'MMPIM ' THEN 'BUYERS' WHEN 'MMPOSM' THEN 'PRESTAMOS FINANCIEROS' END Grupo,O.Cliente1,O.FechaOp,")
SQL.ADD("O.FechaVto,' ' as MarcaSwap,O.OperacionRef,O.FechaOp2,'' Inicio,'' Fin,O.TipoTasa,C.Precio2 Tasa,C.NrCuota,' ' Pata")
SQL.ADD("from "~DBO~".CUOTAS C,"~DBO~".OPERACIONES O")
SQL.ADD("where O.NrOperacion=C.NrOperacion")
IF NoVacio(Dialogo.Lista1)
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Dialogo.Lista1)~")")
ELSE
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(LS:1000))~")")
ENDIF
IF NoVacio(Dialogo.Lista2)
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Dialogo.Lista2)~")")
ELSE
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:1100))~")")
ENDIF
if NoVacio(Dialogo.Cliente1)
 SQL.ADD("and O.Cliente1='"~Dialogo.Cliente1~"'")
endif
if NoVacio(Dialogo.Especie1)
 SQL.ADD("and O.Especie='"~Dialogo.Especie1~"'")
endif
SQL.ADD("and O.TipoOp In ('MMPCLM','MMPEM','MMPIM','MMPOSM')")
SQL.ADD("and O.FechaOp<= '"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("and C.Fecha3 >='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("AND C.Fecha3 >='"~IdxDate(Dialogo.Fecha2)~"'")
IF Dialogo.Fecha3 <> 0
 SQL.ADD("AND C.Fecha3<= '"~IdxDate(Dialogo.Fecha3)~"'")
ENDIF
IF Dialogo.Radio5=0
 SQL.ADD("AND C.Total7 > 0")
ENDIF
SQL.ADD("Union")
SQL.ADD("Select O.Vehiculo1 As Clave,O.Especie,C.Fecha3 Vto,C.Total9 Importe,O.NrOperacion,'CUO' as Tipo,O.TipoOp,'IMP. INTERES' as DescImp,2 as BD1,")
SQL.ADD("CASE O.TipoOp WHEN 'MMPCLM' THEN 'PRESTAMOS FINANCIEROS' WHEN 'MMPEM ' THEN 'BUYERS' WHEN 'MMPIM ' THEN 'BUYERS' WHEN 'MMPOSM' THEN 'PRESTAMOS FINANCIEROS' END Grupo,O.Cliente1,O.FechaOp,")
SQL.ADD("O.FechaVto,' ' as MarcaSwap,O.OperacionRef,O.FechaOp2,'' Inicio,'' Fin,O.TipoTasa,C.Precio2 Tasa,C.NrCuota,' ' Pata")
SQL.ADD("from "~DBO~".CUOTAS C,"~DBO~".OPERACIONES O")
SQL.ADD("where O.NrOperacion=C.NrOperacion")
IF NoVacio(Dialogo.Lista1)
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Dialogo.Lista1)~")")
ELSE
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(LS:1000))~")")
ENDIF
IF NoVacio(Dialogo.Lista2)
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Dialogo.Lista2)~")")
ELSE
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:1100))~")")
ENDIF
if NoVacio(Dialogo.Cliente1)
 SQL.ADD("and O.Cliente1='"~Dialogo.Cliente1~"'")
endif
if NoVacio(Dialogo.Especie1)
 SQL.ADD("and O.Especie='"~Dialogo.Especie1~"'")
endif
SQL.ADD("and O.TipoOp In ('MMPCLM','MMPEM','MMPIM','MMPOSM')")
SQL.ADD("and O.FechaOp<= '"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("and C.Fecha3 >='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("AND C.Fecha3 >='"~IdxDate(Dialogo.Fecha2)~"'")
IF Dialogo.Fecha3 <> 0
 SQL.ADD("AND C.Fecha3<= '"~IdxDate(Dialogo.Fecha3)~"'")
ENDIF
IF Dialogo.Radio5=0
 SQL.ADD("AND C.Total9 > 0")
ENDIF
SQL.ADD("Union")
SQL.ADD("Select O.Vehiculo1 As Clave,O.Especie,C.Fecha3 Vto,(C.Cantidad7 + C.Cantidad6) Importe,O.NrOperacion,'CUO' as Tipo,O.TipoOp,")
SQL.ADD("'COMISIONES' as DescImp,2 as BD1,CASE O.TipoOp WHEN 'MMPCLM' THEN 'PRESTAMOS FINANCIEROS' WHEN ")
SQL.ADD("'MMPEM ' THEN 'BUYERS' WHEN 'MMPIM ' THEN 'BUYERS' WHEN 'MMPOSM' THEN 'PRESTAMOS FINANCIEROS' END Grupo,")
SQL.ADD("O.Cliente1,O.FechaOp,O.FechaVto,' ' as MarcaSwap,O.OperacionRef,O.FechaOp2,'' Inicio,'' Fin,O.TipoTasa,C.Precio2 Tasa,C.NrCuota,' ' Pata")
SQL.ADD("from "~DBO~".CUOTAS C,"~DBO~".OPERACIONES O")
SQL.ADD("where O.NrOperacion=C.NrOperacion")
IF NoVacio(Dialogo.Lista1)
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Dialogo.Lista1)~")")
ELSE
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(LS:1000))~")")
ENDIF
IF NoVacio(Dialogo.Lista2)
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Dialogo.Lista2)~")")
ELSE
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:1100))~")")
ENDIF
if NoVacio(Dialogo.Cliente1)
 SQL.ADD("and O.Cliente1='"~Dialogo.Cliente1~"'")
endif
if NoVacio(Dialogo.Especie1)
 SQL.ADD("and O.Especie='"~Dialogo.Especie1~"'")
endif
SQL.ADD("and O.TipoOp In ('MMPCLM','MMPEM','MMPIM','MMPOSM')")
SQL.ADD("and O.FechaOp<= '"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("and C.Fecha3 >='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("AND C.Fecha3 >='"~IdxDate(Dialogo.Fecha2)~"'")
IF Dialogo.Fecha3 <> 0
 SQL.ADD("AND C.Fecha3<= '"~IdxDate(Dialogo.Fecha3)~"'")
ENDIF
SQL.ADD("AND (C.Cantidad7 + C.Cantidad6) > 0")
SQL.ADD("Union")
SQL.ADD("Select O.Vehiculo1 As Clave,O.Especie,O.FechaVto2 Vto,O.TotalAux3 Importe,O.NrOperacion,'OPE' as Tipo,O.TipoOp,'WHT' as DescImp,2 as BD1,")
SQL.ADD("CASE O.TipoOp WHEN 'MMFRNP' THEN 'F.R.N. / YANKEE' ELSE  'PRESTAMOS SINDICADOS' END Grupo,")
SQL.ADD("O.Cliente1,O.FechaOp,O.FechaVto,' ' as MarcaSwap,O.OperacionRef,O.FechaOp2,'' Inicio,'' Fin,O.TipoTasa,O.Precio1 Tasa,0 NrCuota,' ' Pata")
SQL.ADD("from "~DBO~".OPERACIONES O")
SQL.ADD("where O.TipoOp In ('MMFRNP','MMPSI')")
IF NoVacio(Dialogo.Lista1)
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Dialogo.Lista1)~")")
ELSE
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(LS:1000))~")")
ENDIF
IF NoVacio(Dialogo.Lista2)
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Dialogo.Lista2)~")")
ELSE
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:1100))~")")
ENDIF
if NoVacio(Dialogo.Cliente1)
 SQL.ADD("and O.Cliente1='"~Dialogo.Cliente1~"'")
endif
if NoVacio(Dialogo.Especie1)
 SQL.ADD("and O.Especie='"~Dialogo.Especie1~"'")
endif
SQL.ADD("and O.FechaOp<= '"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("and O.FechaVto2 >='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("AND O.FechaVto2 >='"~IdxDate(Dialogo.Fecha2)~"'")
IF Dialogo.Fecha3 <> 0
 SQL.ADD("AND O.FechaVto2<= '"~IdxDate(Dialogo.Fecha3)~"'")
ENDIF
IF Dialogo.Radio5=0
 SQL.ADD("AND O.TotalAux3 > 0")
ENDIF
SQL.ADD("Union")
SQL.ADD("Select O.Vehiculo1 As Clave,O.Especie,O.FechaVto2 Vto,O.ImporteImp Importe,O.NrOperacion,'OPE' as Tipo,O.TipoOp,'IMP. INTERES' as DescImp,2 as BD1,")
SQL.ADD("CASE O.TipoOp WHEN 'MMFRNP' THEN 'F.R.N. / YANKEE' ELSE 'PRESTAMOS SINDICADOS' END Grupo,")
SQL.ADD("O.Cliente1,O.FechaOp,O.FechaVto,' ' as MarcaSwap,O.OperacionRef,O.FechaOp2,'' Inicio,'' Fin,O.TipoTasa,O.Precio1 Tasa,0 NrCuota,' ' Pata")
SQL.ADD("from "~DBO~".OPERACIONES O")
SQL.ADD("where O.TipoOp In ('MMFRNP','MMPSI')")
IF NoVacio(Dialogo.Lista1)
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Dialogo.Lista1)~")")
ELSE
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(LS:1000))~")")
ENDIF
IF NoVacio(Dialogo.Lista2)
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Dialogo.Lista2)~")")
ELSE
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:1100))~")")
ENDIF
if NoVacio(Dialogo.Cliente1)
 SQL.ADD("and O.Cliente1='"~Dialogo.Cliente1~"'")
endif
if NoVacio(Dialogo.Especie1)
 SQL.ADD("and O.Especie='"~Dialogo.Especie1~"'")
endif
SQL.ADD("and O.FechaOp<= '"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("and O.FechaVto2 >='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("AND O.FechaVto2 >='"~IdxDate(Dialogo.Fecha2)~"'")
IF Dialogo.Fecha3 <> 0
 SQL.ADD("AND O.FechaVto2<= '"~IdxDate(Dialogo.Fecha3)~"'")
ENDIF
IF Dialogo.Radio5=0
 SQL.ADD("AND O.ImporteImp > 0")
ENDIF
SQL.ADD("Union")
SQL.ADD("Select O.Vehiculo1 As Clave,O.Especie,O.FechaVto Vto,O.TotalAux3 Importe,O.NrOperacion,'OPE' as Tipo,O.TipoOp,'WHT' as DescImp,2 as BD1,")
SQL.ADD("CASE O.TipoOp WHEN 'MMPCL ' THEN 'PRESTAMOS FINANCIEROS' WHEN 'MMPE  ' THEN 'BUYERS' WHEN 'MMPI  ' THEN 'BUYERS' WHEN 'MMPCOS' THEN 'PRESTAMOS FINANCIEROS' END Grupo,O.Cliente1,O.FechaOp,")
SQL.ADD("O.FechaVto,' ' as MarcaSwap,O.OperacionRef,O.FechaOp2,'' Inicio,'' Fin,O.TipoTasa,O.TotalAux1 Tasa,0 NrCuota,' ' Pata")
SQL.ADD("From "~DBO~".OPERACIONES O")
SQL.ADD("where O.TipoOp In ('MMPCL','MMPCOS','MMPE','MMPI')")
IF NoVacio(Dialogo.Lista1)
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Dialogo.Lista1)~")")
ELSE
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(LS:1000))~")")
ENDIF
IF NoVacio(Dialogo.Lista2)
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Dialogo.Lista2)~")")
ELSE
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:1100))~")")
ENDIF
if NoVacio(Dialogo.Cliente1)
 SQL.ADD("and O.Cliente1 = '"~Dialogo.Cliente1~"'")
endif
if NoVacio(Dialogo.Especie1)
 SQL.ADD("and O.Especie = '"~Dialogo.Especie1~"'")
endif
SQL.ADD("and O.FechaOp<='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("and O.FechaVto>= '"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("AND O.FechaVto >= '"~IdxDate(Dialogo.Fecha2)~"'")
IF Dialogo.Fecha3 <> 0
 SQL.ADD("AND O.FechaVto<= '"~IdxDate(Dialogo.Fecha3)~"'")
ENDIF
IF Dialogo.Radio5 = 0
 SQL.ADD("AND O.ImporteImp > 0")
ENDIF
SQL.ADD("Union")
SQL.ADD("Select O.Vehiculo1 As Clave,O.Especie,O.FechaVto Vto,O.ImporteImp Importe,O.NrOperacion,'OPE' as Tipo,O.TipoOp,'IMP. INTERES' as DescImp,2 as BD1,")
SQL.ADD("CASE O.TipoOp WHEN 'MMPCL ' THEN 'PRESTAMOS FINANCIEROS' WHEN 'MMPE  ' THEN 'BUYERS' WHEN 'MMPI  ' THEN 'BUYERS' WHEN 'MMPCOS' THEN 'PRESTAMOS FINANCIEROS' END Grupo,O.Cliente1,O.FechaOp,")
SQL.ADD("O.FechaVto,' ' as MarcaSwap,O.OperacionRef,O.FechaOp2,'' Inicio,'' Fin,O.TipoTasa,O.Precio1 Tasa,0 NrCuota,' ' Pata")
SQL.ADD("From "~DBO~".OPERACIONES O")
SQL.ADD("where O.TipoOp In ('MMPCL','MMPCOS','MMPE','MMPI')")
IF NoVacio(Dialogo.Lista1)
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Dialogo.Lista1)~")")
ELSE
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(LS:1000))~")")
ENDIF
IF NoVacio(Dialogo.Lista2)
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Dialogo.Lista2)~")")
ELSE
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:1100))~")")
ENDIF
if NoVacio(Dialogo.Cliente1)
 SQL.ADD("and O.Cliente1='"~Dialogo.Cliente1~"'")
endif
if NoVacio(Dialogo.Especie1)
 SQL.ADD("and O.Especie='"~Dialogo.Especie1~"'")
endif
SQL.ADD("and O.FechaOp<='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("and O.FechaVto>='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("AND O.FechaVto >='"~IdxDate(Dialogo.Fecha2)~"'")
IF Dialogo.Fecha3 <> 0
 SQL.ADD("AND O.FechaVto<= '"~IdxDate(Dialogo.Fecha3)~"'")
ENDIF
IF Dialogo.Radio5=0
 SQL.ADD("AND O.ImporteImp > 0")
ENDIF
SQL.ADD("Union")
SQL.ADD("Select O.Vehiculo1 As Clave,O.Especie,O.FechaVto Vto,O.ImpComCor1 Importe,O.NrOperacion,'OPE' as Tipo,O.TipoOp,'COMISIONES' as DescImp,2 as BD1,")
SQL.ADD("CASE O.TipoOp WHEN 'MMPCL ' THEN 'PRESTAMOS FINANCIEROS' WHEN 'MMPE  ' THEN 'BUYERS' WHEN 'MMPI  ' THEN 'BUYERS' WHEN 'MMPCOS' THEN 'PRESTAMOS FINANCIEROS' END Grupo,O.Cliente1,O.FechaOp,")
SQL.ADD("O.FechaVto,' ' as MarcaSwap,O.OperacionRef,O.FechaOp2,'' Inicio,'' Fin,O.TipoTasa,O.Precio1 Tasa,0 NrCuota,' ' Pata")
SQL.ADD("From "~DBO~".OPERACIONES O")
SQL.ADD("Where O.TipoOp In ('MMPCL','MMPCOS','MMPE','MMPI')")
IF NoVacio(Dialogo.Lista1)
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Dialogo.Lista1)~")")
ELSE
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(LS:1000))~")")
ENDIF
IF NoVacio(Dialogo.Lista2)
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Dialogo.Lista2)~")")
ELSE
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:1100))~")")
ENDIF
if NoVacio(Dialogo.Cliente1)
 SQL.ADD("and O.Cliente1='"~Dialogo.Cliente1~"'")
endif
if NoVacio(Dialogo.Especie1)
 SQL.ADD("and O.Especie='"~Dialogo.Especie1~"'")
endif
SQL.ADD("and O.FechaOp<='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("and O.FechaVto>='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("AND O.FechaVto >='"~IdxDate(Dialogo.Fecha2)~"'")
IF Dialogo.Fecha3 <> 0
 SQL.ADD("AND O.FechaVto<= '"~IdxDate(Dialogo.Fecha3)~"'")
ENDIF
SQL.ADD("AND O.ImpComCor1 > 0")
SQL.ADD("Union")
SQL.ADD("Select O.Vehiculo1 As Clave,O.Especie,O.FechaVto Vto,O.ImporteImp Importe,O.NrOperacion,'OPE' as Tipo,O.TipoOp,'IMP. INTERES' as DescImp,2 as BD1,")
SQL.ADD("'TOMAS ARGENDOLARES CP' Grupo,O.Cliente1,O.FechaOp,O.FechaVto,' ' as MarcaSwap,")
SQL.ADD("O.OperacionRef,O.FechaOp2,'' Inicio,'' Fin,O.TipoTasa,O.Precio1 Tasa,0 NrCuota,' ' Pata")
SQL.ADD("from "~DBO~".OPERACIONES O")
SQL.ADD("where O.TipoOp In ('MMPLME')")
SQL.ADD("AND O.Especie='"~Var('MONEEXTR')~"'")
IF NoVacio(Dialogo.Lista1)
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Dialogo.Lista1)~")")
ELSE
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(LS:1000))~")")
ENDIF
IF NoVacio(Dialogo.Lista2)
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Dialogo.Lista2)~")")
ELSE
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:1100))~")")
ENDIF
if NoVacio(Dialogo.Cliente1)
 SQL.ADD("and O.Cliente1='"~Dialogo.Cliente1~"'")
endif
if NoVacio(Dialogo.Especie1)
 SQL.ADD("and O.Especie='"~Dialogo.Especie1~"'")
endif
SQL.ADD("and O.FechaOp<='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("and O.FechaVto>='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("AND O.FechaVto >='"~IdxDate(Dialogo.Fecha2)~"'")
IF Dialogo.Fecha3 <> 0
 SQL.ADD("AND O.FechaVto<= '"~IdxDate(Dialogo.Fecha3)~"'")
ENDIF
IF Dialogo.Radio5=0
 SQL.ADD("AND O.ImporteImp > 0")
ENDIF
SQL.ADD("Union")
SQL.ADD("Select O.Vehiculo1 As Clave,O.Especie,O.FechaVto Vto,O.ImporteImp Importe,O.NrOperacion,'OPE' as Tipo,O.TipoOp,'IMP. INTERES' as DescImp,2 as BD1,")
SQL.ADD("'PRESTAMOS FINANCIEROS' Grupo,O.Cliente1,O.FechaOp,O.FechaVto,' ' as MarcaSwap,")
SQL.ADD("O.OperacionRef,O.FechaOp2,'' Inicio,'' Fin,O.TipoTasa,O.Precio1 Tasa,0 NrCuota,' ' Pata")
SQL.ADD("from "~DBO~".OPERACIONES O")
SQL.ADD("where O.TipoOp In ('MMPLME')")
SQL.ADD("AND O.Especie <> '"~Var('MONEEXTR')~"'")
IF NoVacio(Dialogo.Lista1)
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Dialogo.Lista1)~")")
ELSE
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(LS:1000))~")")
ENDIF
IF NoVacio(Dialogo.Lista2)
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Dialogo.Lista2)~")")
ELSE
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:1100))~")")
ENDIF
if NoVacio(Dialogo.Cliente1)
 SQL.ADD("and O.Cliente1='"~Dialogo.Cliente1~"'")
endif
if NoVacio(Dialogo.Especie1)
 SQL.ADD("and O.Especie='"~Dialogo.Especie1~"'")
endif
SQL.ADD("and O.FechaOp<='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("and O.FechaVto>='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("AND O.FechaVto >='"~IdxDate(Dialogo.Fecha2)~"'")
IF Dialogo.Fecha3 <> 0
 SQL.ADD("AND O.FechaVto<= '"~IdxDate(Dialogo.Fecha3)~"'")
ENDIF
IF Dialogo.Radio5=0
 SQL.ADD("AND O.ImporteImp > 0")
ENDIF
SQL.ADD("Union")
SQL.ADD("Select O.Vehiculo1 As Clave,O.Especie,C.Fecha1 Vto,C.Importe1 Importe,O.NrOperacion,Tipo,O.TipoOp,'COMISIONES' as DescImp,2 as BD1,")
SQL.ADD("CASE O.TipoOp WHEN 'MMPCL ' THEN 'PRESTAMOS FINANCIEROS' WHEN 'MMPCLM' THEN 'PRESTAMOS FINANCIEROS' WHEN 'MMPCOS' THEN 'PRESTAMOS FINANCIEROS' WHEN 'MMPE  ' THEN 'BUYERS' WHEN ")
SQL.ADD("'MMPEM ' THEN 'BUYERS' WHEN 'MMPI  ' THEN 'BUYERS' WHEN 'MMPIM ' THEN 'BUYERS' WHEN 'MMPEM ' THEN 'BUYERS' WHEN 'MMPOSM' THEN 'PRESTAMOS FINANCIEROS' WHEN 'MMPSI ' THEN 'PRESTAMOS SINDICADOS' WHEN ")
SQL.ADD("'MMSIMV' THEN 'PRESTAMOS SINDICADOS' WHEN 'MMFRNP' THEN 'F.R.N. / YANKEE' WHEN 'MMFIMV' THEN 'F.R.N. / YANKEE' END Grupo,O.Cliente1,O.FechaOp,O.FechaVto,")
SQL.ADD("' ' as MarcaSwap,O.OperacionRef,O.FechaOp2,'' Inicio,'' Fin,O.TipoTasa,1 Tasa,0 NrCuota,' ' Pata")
SQL.ADD("from "~DBO~".COMISIONES2 C,"~DBO~".OPERACIONES O")
SQL.ADD("where O.NrOperacion=C.NrOperacion")
IF NoVacio(Dialogo.Lista1)
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Dialogo.Lista1)~")")
ELSE
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(LS:1000))~")")
ENDIF
IF NoVacio(Dialogo.Lista2)
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Dialogo.Lista2)~")")
ELSE
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:1100))~")")
ENDIF
if NoVacio(Dialogo.Cliente1)
 SQL.ADD("and O.Cliente1='"~Dialogo.Cliente1~"'")
endif
if NoVacio(Dialogo.Especie1)
 SQL.ADD("and O.Especie='"~Dialogo.Especie1~"'")
endif
SQL.ADD("and O.TipoOp In ('MMPCL','MMPCLM','MMPCOS','MMPE','MMPEM','MMPI','MMPIM','MMPOSM','MMPSI','MMSIMV','MMFRNP','MMFIMV') ")
SQL.ADD("and O.FechaOp<='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("and C.Fecha1 >='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("AND C.Fecha1 >='"~IdxDate(Dialogo.Fecha2)~"'")
IF Dialogo.Fecha3 <> 0
 SQL.ADD("AND C.Fecha1<= '"~IdxDate(Dialogo.Fecha3)~"'")
ENDIF
SQL.ADD("and C.Tipo <> '5'")
SQL.ADD("AND C.Importe1 > 0")
SQL.ADD("Union")
SQL.ADD("Select O.Vehiculo1 As Clave,O.Especie,C.Fecha1 Vto,C.Importe1 Importe,O.NrOperacion,Tipo,O.TipoOp,'COMISIONES' as DescImp,2 as BD1,")
SQL.ADD("'TOMAS ARGENDOLARES CP' Grupo,O.Cliente1,O.FechaOp,O.FechaVto,' ' as MarcaSwap,")
SQL.ADD("O.OperacionRef,O.FechaOp2,'' Inicio,'' Fin,O.TipoTasa,1 Tasa,0 NrCuota,' ' Pata")
SQL.ADD("from "~DBO~".COMISIONES2 C,"~DBO~".OPERACIONES O")
SQL.ADD("where O.NrOperacion=C.NrOperacion")
SQL.ADD("AND O.Especie='"~Var('MONEEXTR')~"'")
IF NoVacio(Dialogo.Lista1)
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Dialogo.Lista1)~")")
ELSE
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(LS:1000))~")")
ENDIF
IF NoVacio(Dialogo.Lista2)
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Dialogo.Lista2)~")")
ELSE
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:1100))~")")
ENDIF
if NoVacio(Dialogo.Cliente1)
 SQL.ADD("and O.Cliente1='"~Dialogo.Cliente1~"'")
endif
if NoVacio(Dialogo.Especie1)
 SQL.ADD("and O.Especie='"~Dialogo.Especie1~"'")
endif
SQL.ADD("AND O.TipoOp='MMPLME'")
SQL.ADD("and O.FechaOp<='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("and C.Fecha1 >='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("AND C.Fecha1 >='"~IdxDate(Dialogo.Fecha2)~"'")
IF Dialogo.Fecha3 <> 0
 SQL.ADD("AND C.Fecha1<= '"~IdxDate(Dialogo.Fecha3)~"'")
ENDIF
SQL.ADD("and C.Tipo <> '5'")
SQL.ADD("AND C.Importe1 > 0")
SQL.ADD("Union")
SQL.ADD("Select O.Vehiculo1 As Clave,O.Especie,C.Fecha1 Vto,C.Importe1 Importe,O.NrOperacion,")
SQL.ADD("Tipo,O.TipoOp,'COMISIONES' as DescImp,2 as BD1,'PRESTAMOS FINANCIEROS' Grupo,O.Cliente1,")
SQL.ADD("O.FechaOp,O.FechaVto,' ' as MarcaSwap,O.OperacionRef,O.FechaOp2,'' Inicio,'' Fin,O.TipoTasa,1 Tasa,0 NrCuota,' ' Pata")
SQL.ADD("from "~DBO~".COMISIONES2 C,"~DBO~".OPERACIONES O")
SQL.ADD("where O.NrOperacion=C.NrOperacion")
SQL.ADD("AND O.Especie <> '"~Var('MONEEXTR')~"'")
IF NoVacio(Dialogo.Lista1)
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Dialogo.Lista1)~")")
ELSE
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(LS:1000))~")")
ENDIF
IF NoVacio(Dialogo.Lista2)
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Dialogo.Lista2)~")")
ELSE
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:1100))~")")
ENDIF
if NoVacio(Dialogo.Cliente1)
 SQL.ADD("and O.Cliente1='"~Dialogo.Cliente1~"'")
endif
if NoVacio(Dialogo.Especie1)
 SQL.ADD("and O.Especie='"~Dialogo.Especie1~"'")
endif
SQL.ADD("AND O.TipoOp='MMPLME'")
SQL.ADD("and O.FechaOp<='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("and C.Fecha1 >='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("AND C.Fecha1 >='"~IdxDate(Dialogo.Fecha2)~"'")
IF Dialogo.Fecha3 <> 0
 SQL.ADD("AND C.Fecha1<= '"~IdxDate(Dialogo.Fecha3)~"'")
ENDIF
SQL.ADD("and C.Tipo <> '5'")
SQL.ADD("AND C.Importe1 > 0")
SQL.ADD("Union")
SQL.ADD("Select O.Vehiculo1 As Clave,O.Especie,C.Fecha1 Vto,C.Importe1 Importe,O.NrOperacion,")
SQL.ADD("Tipo,O.TipoOp,'COMISIONES' as DescImp,2 as BD1,'O.N. EN ' Grupo,O.Cliente1,O.FechaOp,")
SQL.ADD("O.FechaVto,' ' as MarcaSwap,O.OperacionRef,O.FechaOp2,'' Inicio,'' Fin,O.TipoTasa,1 Tasa,0 NrCuota,' ' Pata")
SQL.ADD("from "~DBO~".COMISIONES2 C,"~DBO~".OPERACIONES O")
SQL.ADD("where O.NrOperacion=C.NrOperacion")
IF NoVacio(Dialogo.Lista1)
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Dialogo.Lista1)~")")
ELSE
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(LS:1000))~")")
ENDIF
IF NoVacio(Dialogo.Lista2)
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Dialogo.Lista2)~")")
ELSE
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:1100))~")")
ENDIF
if NoVacio(Dialogo.Cliente1)
 SQL.ADD("and O.Cliente1='"~Dialogo.Cliente1~"'")
endif
if NoVacio(Dialogo.Especie1)
 SQL.ADD("and O.Especie='"~Dialogo.Especie1~"'")
endif
SQL.ADD("and O.TipoOp In ('MMON','MMONM')")
SQL.ADD("and O.FechaOp<='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("and C.Fecha1 >='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("AND C.Fecha1 >='"~IdxDate(Dialogo.Fecha2)~"'")
IF Dialogo.Fecha3 <> 0
 SQL.ADD("AND C.Fecha1<= '"~IdxDate(Dialogo.Fecha3)~"'")
ENDIF
SQL.ADD("and C.Tipo <> '5'")
SQL.ADD("AND C.Importe1 > 0")
SQL.ADD("Union")
SQL.ADD("Select O.Vehiculo1 As Clave,O.Especie,O.FechaVto2 Vto,O.TotalAux3 Importe,O.NrOperacion,")
SQL.ADD("'OPE' as Tipo,O.TipoOp,'WHT' as DescImp,2 as BD1,'O.N. EN ' Grupo,O.Cliente1,O.FechaOp,O.FechaVto,")
SQL.ADD("' ' as MarcaSwap,O.OperacionRef,O.FechaOp2,'' Inicio,'' Fin,O.TipoTasa,O.Precio1 Tasa,0 NrCuota,' ' Pata")
SQL.ADD("from "~DBO~".OPERACIONES O")
SQL.ADD("where O.TipoOp In ('MMON')")
IF NoVacio(Dialogo.Lista1)
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Dialogo.Lista1)~")")
ELSE
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(LS:1000))~")")
ENDIF
IF NoVacio(Dialogo.Lista2)
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Dialogo.Lista2)~")")
ELSE
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:1100))~")")
ENDIF
if NoVacio(Dialogo.Cliente1)
 SQL.ADD("and O.Cliente1='"~Dialogo.Cliente1~"'")
endif
if NoVacio(Dialogo.Especie1)
 SQL.ADD("and O.Especie='"~Dialogo.Especie1~"'")
endif
SQL.ADD("and O.FechaOp<='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("and O.FechaVto2>='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("AND O.FechaVto2>='"~IdxDate(Dialogo.Fecha2)~"'")
IF Dialogo.Fecha3 <> 0
 SQL.ADD("AND O.FechaVto2<= '"~IdxDate(Dialogo.Fecha3)~"'")
ENDIF
IF Dialogo.Radio5=0
 SQL.ADD("AND O.TotalAux3 > 0")
ENDIF
SQL.ADD("Union")
SQL.ADD("Select O.Vehiculo1 As Clave,O.Especie,O.FechaVto2 Vto,O.ImporteImp Importe,")
SQL.ADD("O.NrOperacion,'OPE' as Tipo,O.TipoOp,'IMP. INTERES' as DescImp,2 as BD1,")
SQL.ADD("'O.N. EN ' Grupo,O.Cliente1,O.FechaOp,O.FechaVto,' ' as MarcaSwap,O.OperacionRef,")
SQL.ADD("O.FechaOp2,'' Inicio,'' Fin,O.TipoTasa,O.Precio1 Tasa,0 NrCuota,' ' Pata")
SQL.ADD("from "~DBO~".OPERACIONES O")
SQL.ADD("where O.TipoOp In ('MMON')")
IF NoVacio(Dialogo.Lista1)
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Dialogo.Lista1)~")")
ELSE
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(LS:1000))~")")
ENDIF
IF NoVacio(Dialogo.Lista2)
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Dialogo.Lista2)~")")
ELSE
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:1100))~")")
ENDIF
if NoVacio(Dialogo.Cliente1)
 SQL.ADD("and O.Cliente1='"~Dialogo.Cliente1~"'")
endif
if NoVacio(Dialogo.Especie1)
 SQL.ADD("and O.Especie='"~Dialogo.Especie1~"'")
endif
SQL.ADD("and O.FechaOp<='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("and O.FechaVto2>='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("AND O.FechaVto2>='"~IdxDate(Dialogo.Fecha2)~"'")
IF Dialogo.Fecha3 <> 0
 SQL.ADD("AND O.FechaVto2<= '"~IdxDate(Dialogo.Fecha3)~"'")
ENDIF
IF Dialogo.Radio5=0
 SQL.ADD("AND O.ImporteImp > 0")
ENDIF
SQL.ADD("Union")
SQL.ADD("Select O.Vehiculo1 As Clave,O.Especie,C.Fecha3 Vto,C.Precio3 Importe,O.NrOperacion,")
SQL.ADD("'CUO' as Tipo,O.TipoOp,'WHT' as DescImp,2 as BD1,'O.N. EN ' Grupo,O.Cliente1,O.FechaOp,")
SQL.ADD("O.FechaVto,' ' as MarcaSwap,O.OperacionRef,O.FechaOp2,'' Inicio,'' Fin,O.TipoTasa,C.Precio2 Tasa,C.NrCuota,' ' Pata")
SQL.ADD("from "~DBO~".CUOTAS C,"~DBO~".OPERACIONES O")
SQL.ADD("where O.NrOperacion=C.NrOperacion")
IF NoVacio(Dialogo.Lista1)
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Dialogo.Lista1)~")")
ELSE
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(LS:1000))~")")
ENDIF
IF NoVacio(Dialogo.Lista2)
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Dialogo.Lista2)~")")
ELSE
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:1100))~")")
ENDIF
if NoVacio(Dialogo.Cliente1)
 SQL.ADD("and O.Cliente1='"~Dialogo.Cliente1~"'")
endif
if NoVacio(Dialogo.Especie1)
 SQL.ADD("and O.Especie='"~Dialogo.Especie1~"'")
endif
SQL.ADD("and O.TipoOp In ('MMONM')")
SQL.ADD("and O.FechaOp<='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("and C.Fecha3>='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("AND C.Fecha3 >='"~IdxDate(Dialogo.Fecha2)~"'")
IF Dialogo.Fecha3 <> 0
 SQL.ADD("AND C.Fecha3<= '"~IdxDate(Dialogo.Fecha3)~"'")
ENDIF
IF Dialogo.Radio5=0
 SQL.ADD("AND C.Precio3 > 0")
ENDIF
SQL.ADD("Union")
SQL.ADD("Select O.Vehiculo1 As Clave,O.Especie,C.Fecha3 Vto,C.Total9 Importe,O.NrOperacion,'CUO' as Tipo,O.TipoOp,'IMP. INTERES' as DescImp,2 as BD1,")
SQL.ADD("'O.N. EN ' Grupo,O.Cliente1,O.FechaOp,O.FechaVto,' ' as MarcaSwap,O.OperacionRef,")
SQL.ADD("O.FechaOp2,'' Inicio,'' Fin,O.TipoTasa,C.Precio2 Tasa,C.NrCuota,' ' Pata")
SQL.ADD("from "~DBO~".CUOTAS C,"~DBO~".OPERACIONES O")
SQL.ADD("where O.NrOperacion=C.NrOperacion")
IF NoVacio(Dialogo.Lista1)
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Dialogo.Lista1)~")")
ELSE
 SQL.ADD("AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(LS:1000))~")")
ENDIF
IF NoVacio(Dialogo.Lista2)
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Dialogo.Lista2)~")")
ELSE
 SQL.ADD("AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:1100))~")")
ENDIF
if NoVacio(Dialogo.Cliente1)
 SQL.ADD("and O.Cliente1='"~Dialogo.Cliente1~"'")
endif
if NoVacio(Dialogo.Especie1)
 SQL.ADD("and O.Especie='"~Dialogo.Especie1~"'")
endif
SQL.ADD("and O.TipoOp In ('MMONM')")
SQL.ADD("and O.FechaOp<='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("and C.Fecha3>='"~IdxDate(Dialogo.Fecha1)~"'")
SQL.ADD("AND C.Fecha3>='"~IdxDate(Dialogo.Fecha2)~"'")
IF Dialogo.Fecha3 <> 0
 SQL.ADD("AND C.Fecha3<= '"~IdxDate(Dialogo.Fecha3)~"'")
ENDIF
IF Dialogo.Radio5=0
 SQL.ADD("AND C.Total9 > 0")
ENDIF
(.'CAIDA8C2'.)