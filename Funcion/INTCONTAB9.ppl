*- MMPCL,MMPE,MMPI,MMPCOS
*- MMFIMV Definitivos
SQL.ADD(" Select DISTINCT O.TipoOp ")
SQL.ADD(" from "~DBO~".CUOTAS C, "~DBO~".OPERACIONES  O ")
SQL.ADD(" where O.NrOperacion = C.NrOperacion ")
If NoVacio(Dialogo.NrOperacion1)
  SQL.ADD(" AND O.NrOperacion='"~Dialogo.NrOperacion1~"' ")
EndIf
SQL.ADD(" AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(VH:10))~") ")
if NoVacio(Dialogo.Especie1)
   SQL.ADD(" and O.Especie = '"~Dialogo.Especie1~"' ")
endif
if NoVacio(Dialogo.Lista2)
   SQL.ADD(" and O.TipoOp In ("~Pipe2Comillas(Dialogo.Lista2)~") ")
ELSE
   SQL.ADD(" AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:2))~") ")
EndIf
SQL.ADD(" and O.TipoOp In ('MMFIMV') and O.RB5='0' ")
SQL.ADD(" and O.FechaOp<='"~IdxDate(Dialogo.Fecha1)~"' ")
*- Que Vencen en ese mes y anio aunque no esten vigentes
SQL.ADD(" and ((O.FechaVto>'"~IdxDate(Dialogo.Fecha1)~"' ")

SQL.ADD(" and C.Fecha1<='"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and C.Fecha3>'"~IdxDate(Dialogo.Fecha1)~"') ")
SQL.ADD(" or (Year(C.Fecha3)="~Fstr(Anio(Dialogo.Fecha1),0,0)~" ")
SQL.ADD(" and Month(C.Fecha3)="~Fstr(Mes(Dialogo.Fecha1),0,0)~") ")
SQL.ADD(" ) ")


SQL.ADD(" Union ")
*- MMFIMV No Definitivos
SQL.ADD(" Select DISTINCT O.TipoOp ")
SQL.ADD(" from "~DBO~".CUOTAS C, "~DBO~".OPERACIONES  O ")
SQL.ADD(" where O.NrOperacion = C.NrOperacion ")
If NoVacio(Dialogo.NrOperacion1)
  SQL.ADD(" AND O.NrOperacion='"~Dialogo.NrOperacion1~"' ")
EndIf
SQL.ADD(" AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(VH:10))~") ")
if NoVacio(Dialogo.Especie1)
   SQL.ADD(" and O.Especie = '"~Dialogo.Especie1~"' ")
endif
if NoVacio(Dialogo.Lista2)
   SQL.ADD(" and O.TipoOp In ("~Pipe2Comillas(Dialogo.Lista2)~") ")
ELSE
   SQL.ADD(" AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:2))~") ")
EndIf
SQL.ADD(" and O.TipoOp In ('MMFIMV') and O.RB5='1' ")
SQL.ADD(" and O.FechaOp<='"~IdxDate(Dialogo.Fecha1)~"' ")
*- Que Vencen en ese mes aunque no esten vigentes
SQL.ADD(" and (O.FechaVto>'"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" or (Year(O.FechaVto)="~Fstr(Anio(Dialogo.Fecha1),0,0)~" ")
SQL.ADD(" and Month(O.FechaVto)="~Fstr(Mes(Dialogo.Fecha1),0,0)~") ")
SQL.ADD(" ) ")

SQL.ADD(" Union ")

*- MMFRNP, Prestamos Locales y OffShore, Postfinanciacion, PreFinanciacion
SQL.ADD(" Select DISTINCT O.TipoOp ")
SQL.ADD(" from "~DBO~".OPERACIONES  O Where ")
SQL.ADD(" O.TipoOp In ('MMFRNP','MMPCL','MMPCOS','MMPI','MMPE') ")
SQL.ADD(" and O.FechaOp<='"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and (O.FechaVto>'"~IdxDate(Dialogo.Fecha1)~"' ")
*- Que Vencen en ese mes aunque no esten vigentes
SQL.ADD(" or (Year(O.FechaVto)="~Fstr(Anio(Dialogo.Fecha1),0,0)~" ")
SQL.ADD(" and Month(O.FechaVto)="~Fstr(Mes(Dialogo.Fecha1),0,0)~") ")
SQL.ADD(" ) ")
If NoVacio(Dialogo.NrOperacion1)
  SQL.ADD(" AND O.NrOperacion='"~Dialogo.NrOperacion1~"' ")
EndIf
SQL.ADD(" AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(VH:10))~") ")
if NoVacio(Dialogo.Especie1)
   SQL.ADD(" and O.Especie = '"~Dialogo.Especie1~"' ")
endif
if NoVacio(Dialogo.Lista2)
   SQL.ADD(" and O.TipoOp In ("~Pipe2Comillas(Dialogo.Lista2)~") ")
ELSE
   SQL.ADD(" AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:2))~") ")
EndIf

SQL.ADD(" Union ")

*- MMPLME
SQL.ADD(" Select DISTINCT O.TipoOp ")
SQL.ADD(" from "~DBO~".OPERACIONES  O Where ")
SQL.ADD(" O.TipoOp In ('MMPLME') ")
SQL.ADD(" and O.FechaOp<='"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and (O.FechaVto>'"~IdxDate(Dialogo.Fecha1)~"' ")
*- Que Vencen en ese mes aunque no esten vigentes
SQL.ADD(" or (Year(O.FechaVto)="~Fstr(Anio(Dialogo.Fecha1),0,0)~" ")
SQL.ADD(" and Month(O.FechaVto)="~Fstr(Mes(Dialogo.Fecha1),0,0)~") ")
SQL.ADD(" ) ")
If NoVacio(Dialogo.NrOperacion1)
  SQL.ADD(" AND O.NrOperacion='"~Dialogo.NrOperacion1~"' ")
EndIf
SQL.ADD(" AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(VH:10))~") ")
if NoVacio(Dialogo.Especie1)
   SQL.ADD(" and O.Especie = '"~Dialogo.Especie1~"' ")
endif
if NoVacio(Dialogo.Lista2)
   SQL.ADD(" and O.TipoOp In ("~Pipe2Comillas(Dialogo.Lista2)~") ")
ELSE
   SQL.ADD(" AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:2))~") ")
EndIf

SQL.ADD(" Union")

*- MMPCLM,MMPOSM,MMPEM,MMPIM
SQL.ADD(" Select DISTINCT O.TipoOp ")
SQL.ADD("from "~DBO~".CUOTAS C, "~DBO~".OPERACIONES  O ")
SQL.ADD("where O.NrOperacion = C.NrOperacion ")
If NoVacio(Dialogo.NrOperacion1)
  SQL.ADD(" AND O.NrOperacion='"~Dialogo.NrOperacion1~"' ")
EndIf
SQL.ADD(" AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(VH:10))~") ")
if NoVacio(Dialogo.Especie1)
   SQL.ADD(" and O.Especie = '"~Dialogo.Especie1~"' ")
endif
if NoVacio(Dialogo.Lista2)
   SQL.ADD(" and O.TipoOp In ("~Pipe2Comillas(Dialogo.Lista2)~") ")
ELSE
   SQL.ADD(" AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:2))~") ")
EndIf
SQL.ADD(" and O.TipoOp In ('MMPCLM','MMPOSM','MMPIM','MMPEM') ")
SQL.ADD(" and O.FechaOp<='"~IdxDate(Dialogo.Fecha1)~"' ")
*- Que Vencen en ese mes aunque no esten vigentes
SQL.ADD(" and ((O.FechaVto>'"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and C.Fecha1<='"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and C.Fecha2>'"~IdxDate(Dialogo.Fecha1)~"') ")
SQL.ADD(" or (Year(C.Fecha2)="~Fstr(Anio(Dialogo.Fecha1),0,0)~" ")
SQL.ADD(" and Month(C.Fecha2)="~Fstr(Mes(Dialogo.Fecha1),0,0)~") ")
SQL.ADD(" ) ")

SQL.ADD(" UNION ")

*- ON Unico Vto
SQL.ADD(" Select DISTINCT O.TipoOp ")
SQL.ADD(" from "~DBO~".OPERACIONES O ")
SQL.ADD("where O.FechaOp  <= '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD("       and (O.FechaVto > '"~IdxDate(Dialogo.Fecha1)~"' ")
*- Que Vencen en ese mes y anio aunque no esten vigentes
SQL.ADD("       or (Year(O.FechaVto)="~Fstr(Anio(Dialogo.Fecha1),0,0)~" ")
SQL.ADD("       and Month(O.FechaVto)="~Fstr(Mes(Dialogo.Fecha1),0,0)~") ")
SQL.ADD(" ) ")
SQL.ADD("  and O.TipoOp In ('MMON') ")
If NoVacio(Dialogo.NrOperacion1)
  SQL.ADD(" AND O.NrOperacion='"~Dialogo.NrOperacion1~"' ")
EndIf
SQL.ADD(" AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(VH:10))~") ")
if NoVacio(Dialogo.Especie1)
   SQL.ADD(" and O.Especie = '"~Dialogo.Especie1~"' ")
endif
if NoVacio(Dialogo.Lista2)
   SQL.ADD(" and O.TipoOp In ("~Pipe2Comillas(Dialogo.Lista2)~") ")
ELSE
   SQL.ADD(" AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:2))~") ")
EndIf

*- PS Unico Vto
SQL.ADD("Union ")
SQL.ADD(" Select DISTINCT O.TipoOp ")
SQL.ADD(" from "~DBO~".OPERACIONES O ")
SQL.ADD("where O.FechaOp  <= '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD("       and (O.FechaVto > '"~IdxDate(Dialogo.Fecha1)~"' ")
*- Que Vencen en ese mes y anio aunque no esten vigentes
SQL.ADD("       or (Year(O.FechaVto)="~Fstr(Anio(Dialogo.Fecha1),0,0)~" ")
SQL.ADD("       and Month(O.FechaVto)="~Fstr(Mes(Dialogo.Fecha1),0,0)~") ")
SQL.ADD(" ) ")
SQL.ADD("  and O.TipoOp In ('MMPSI','MMPSC') ")
If NoVacio(Dialogo.NrOperacion1)
  SQL.ADD(" AND O.NrOperacion='"~Dialogo.NrOperacion1~"' ")
EndIf
SQL.ADD(" AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(VH:10))~") ")
if NoVacio(Dialogo.Especie1)
   SQL.ADD(" and O.Especie = '"~Dialogo.Especie1~"' ")
endif
if NoVacio(Dialogo.Lista2)
   SQL.ADD(" and O.TipoOp In ("~Pipe2Comillas(Dialogo.Lista2)~") ")
ELSE
   SQL.ADD(" AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:2))~") ")
EndIf


SQL.ADD("Union ")
*- ON MVto.

SQL.ADD(" Select DISTINCT O.TipoOp ")
SQL.ADD(" from "~DBO~".OPERACIONES O, "~DBO~".CUOTAS C ")
SQL.ADD("where O.NrOperacion = C.NrOperacion ")
SQL.ADD("  and O.FechaOp  <= '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD("       and (O.FechaVto > '"~IdxDate(Dialogo.Fecha1)~"' ")
*- Que Vencen en ese mes y anio aunque no esten vigentes
SQL.ADD("       or (Year(O.FechaVto)="~Fstr(Anio(Dialogo.Fecha1),0,0)~" ")
SQL.ADD("       and Month(O.FechaVto)="~Fstr(Mes(Dialogo.Fecha1),0,0)~") ")
SQL.ADD(" ) ")
SQL.ADD("  and O.TipoOp In ('MMONM') ")
If NoVacio(Dialogo.NrOperacion1)
  SQL.ADD(" AND O.NrOperacion='"~Dialogo.NrOperacion1~"' ")
EndIf
SQL.ADD(" AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(VH:10))~") ")
if NoVacio(Dialogo.Especie1)
   SQL.ADD(" and O.Especie = '"~Dialogo.Especie1~"' ")
endif
if NoVacio(Dialogo.Lista2)
   SQL.ADD(" and O.TipoOp In ("~Pipe2Comillas(Dialogo.Lista2)~") ")
ELSE
   SQL.ADD(" AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:2))~") ")
EndIf

*- PS Mv
SQL.ADD("Union ")

SQL.ADD(" Select DISTINCT O.TipoOp ")
SQL.ADD(" from "~DBO~".OPERACIONES O, "~DBO~".CUOTAS C ")
SQL.ADD("where O.NrOperacion = C.NrOperacion ")
SQL.ADD("  and O.FechaOp<='"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD("  and (O.FechaVto>'"~IdxDate(Dialogo.Fecha1)~"' ")
*- Que Vencen en ese mes y anio aunque no esten vigentes
SQL.ADD(" or (Year(O.FechaVto)="~Fstr(Anio(Dialogo.Fecha1),0,0)~" ")
SQL.ADD(" and Month(O.FechaVto)="~Fstr(Mes(Dialogo.Fecha1),0,0)~") ")
SQL.ADD(" ) ")

SQL.ADD("  and O.TipoOp In ('MMSIMV','MMSCMV') ")
If NoVacio(Dialogo.NrOperacion1)
  SQL.ADD(" AND O.NrOperacion='"~Dialogo.NrOperacion1~"' ")
EndIf
SQL.ADD(" AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(VH:10))~") ")
if NoVacio(Dialogo.Especie1)
   SQL.ADD(" and O.Especie = '"~Dialogo.Especie1~"' ")
endif
if NoVacio(Dialogo.Lista2)
   SQL.ADD(" and O.TipoOp In ("~Pipe2Comillas(Dialogo.Lista2)~") ")
ELSE
   SQL.ADD(" AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:2))~") ")
EndIf

SQL.ADD(" UNION ")

******** SECCION VEHICULO DE MULTIPLE VENCIMIENTO *****************
SQL.ADD(" Select DISTINCT O.TipoOp ")
SQL.ADD(" From   "~DBO~".CUOTAS C, "~DBO~".OPERACIONES O ")
SQL.ADD(" Where  O.NrOperacion = C.NrOperacion ")
If NoVacio(Dialogo.NrOperacion1)
  SQL.ADD(" AND O.NrOperacion='"~Dialogo.NrOperacion1~"' ")
EndIf
SQL.ADD(" AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(VH:10))~") ")
if NoVacio(Dialogo.Especie1)
   SQL.ADD(" and O.Especie = '"~Dialogo.Especie1~"' ")
endif
if NoVacio(Dialogo.Lista2)
   SQL.ADD(" and O.TipoOp In ("~Pipe2Comillas(Dialogo.Lista2)~") ")
ELSE
   SQL.ADD(" AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:2))~") ")
EndIf
SQL.ADD("        AND O.TipoOp IN ('DTSFFM','DTSFVM','DTSVVM') ")
SQL.ADD("        AND C.Fecha1<='"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD("        and (C.Fecha2>'"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD("            or (Year(C.Fecha2)="~Fstr(Anio(Dialogo.Fecha1),0,0)~" ")
SQL.ADD("            and Month(C.Fecha2)="~Fstr(Mes(Dialogo.Fecha1),0,0)~")) ")
SQL.ADD("        AND C.Cantidad3 > 0 ")

SQL.ADD(" UNION ")

******** SECCION CONTRAPARTE DE MULTIPLE VENCIMIENTO *****************
SQL.ADD(" Select DISTINCT O.TipoOp ")
SQL.ADD(" From   "~DBO~".CUOTAS C, "~DBO~".OPERACIONES O ")
SQL.ADD(" Where  O.NrOperacion = C.NrOperacion ")
If NoVacio(Dialogo.NrOperacion1)
  SQL.ADD(" AND O.NrOperacion='"~Dialogo.NrOperacion1~"' ")
EndIf
SQL.ADD(" AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(VH:10))~") ")
if NoVacio(Dialogo.Especie1)
   SQL.ADD(" and O.Especie = '"~Dialogo.Especie1~"' ")
endif
if NoVacio(Dialogo.Lista2)
   SQL.ADD(" and O.TipoOp In ("~Pipe2Comillas(Dialogo.Lista2)~") ")
ELSE
   SQL.ADD(" AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:2))~") ")
EndIf
SQL.ADD("        AND O.TipoOp IN ('DTSFFM','DTSFVM','DTSVVM') ")
SQL.ADD("        AND C.Fecha5<='"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD("        and (C.Fecha6>'"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD("            or (Year(C.Fecha6)="~Fstr(Anio(Dialogo.Fecha1),0,0)~" ")
SQL.ADD("            and Month(C.Fecha6)="~Fstr(Mes(Dialogo.Fecha1),0,0)~")) ")
SQL.ADD("        AND C.Cantidad4 > 0 ")
SQL.ADD(" UNION ")

******** SECCION VEHICULO DE UNICO VENCIMIENTO *****************
SQL.ADD(" Select DISTINCT TipoOp ")
SQL.ADD(" From   "~DBO~".OPERACIONES ")
SQL.ADD(" Where  TipoOp IN ('DTSFF','DTSFV','DTSVV') ")
If NoVacio(Dialogo.NrOperacion1)
  SQL.ADD(" AND NrOperacion='"~Dialogo.NrOperacion1~"' ")
EndIf
SQL.ADD(" AND Vehiculo1 IN ("~Pipe2Comillas(Val(VH:10))~") ")
if NoVacio(Dialogo.Especie1)
   SQL.ADD(" and Especie = '"~Dialogo.Especie1~"' ")
endif
if NoVacio(Dialogo.Lista2)
   SQL.ADD(" and TipoOp In ("~Pipe2Comillas(Dialogo.Lista2)~") ")
ELSE
   SQL.ADD(" AND TipoOp IN ("~Pipe2Comillas(Val(LS:2))~") ")
EndIf
SQL.ADD("        AND FechaOp2<='"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD("        and (FechaEj>'"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD("            or (Year(FechaEj)="~Fstr(Anio(Dialogo.Fecha1),0,0)~" ")
SQL.ADD("            and Month(FechaEj)="~Fstr(Mes(Dialogo.Fecha1),0,0)~")) ")
SQL.ADD("        AND TotalIntereses > 0 ")
SQL.ADD(" UNION ")

******** SECCION CONTRAPARTE DE UNICO VENCIMIENTO *****************
SQL.ADD(" Select DISTINCT TipoOp ")
SQL.ADD(" From   "~DBO~".OPERACIONES ")
SQL.ADD(" Where  TipoOp IN ('DTSFF','DTSFV','DTSVV') ")
If NoVacio(Dialogo.NrOperacion1)
  SQL.ADD(" AND NrOperacion='"~Dialogo.NrOperacion1~"' ")
EndIf
SQL.ADD(" AND Vehiculo1 IN ("~Pipe2Comillas(Val(VH:10))~") ")
if NoVacio(Dialogo.Especie1)
   SQL.ADD(" and Especie = '"~Dialogo.Especie1~"' ")
endif
if NoVacio(Dialogo.Lista2)
   SQL.ADD(" and TipoOp In ("~Pipe2Comillas(Dialogo.Lista2)~") ")
ELSE
   SQL.ADD(" AND TipoOp IN ("~Pipe2Comillas(Val(LS:2))~") ")
EndIf
SQL.ADD("        AND FechaOp2<='"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD("        and (FechaEj>'"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD("            or (Year(FechaEj)="~Fstr(Anio(Dialogo.Fecha1),0,0)~" ")
SQL.ADD("            and Month(FechaEj)="~Fstr(Mes(Dialogo.Fecha1),0,0)~")) ")
SQL.ADD("        AND InteresesCorr > 0 ")

SQL.ADD(" UNION ")

SQL.ADD(" Select DISTINCT TipoOp ")
SQL.ADD(" from "~DBO~".OPERACIONES O ")
SQL.ADD("where O.FechaOp  <= '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD("  and (O.FechaVto > '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" or (Year(O.FechaVto)="~Fstr(Anio(Dialogo.Fecha1),0,0)~" ")
SQL.ADD(" and Month(O.FechaVto)="~Fstr(Mes(Dialogo.Fecha1),0,0)~") ")
SQL.ADD(" ) ")
SQL.ADD(" and  O.TipoOp In ('MMCP','MMDCA','MMO','MMD','MMCCR') ")
if NoVacio(Dialogo.Especie1)
   SQL.ADD(" and O.Especie = '"~Dialogo.Especie1~"' ")
endif
if NoVacio(Dialogo.Lista2)
   SQL.ADD(" and TipoOp In ("~Pipe2Comillas(Dialogo.Lista2)~") ")
ELSE
   SQL.ADD(" AND TipoOp IN ("~Pipe2Comillas(Val(LS:2))~") ")
EndIf
SQL.ADD(" AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(VH:10))~") ")
If NoVacio(Dialogo.NrOperacion1)
   SQL.ADD("  and O.NrOperacion='"~Dialogo.NrOperacion1~"' ")
EndIf

**********************
SQL.ADD(" UNION ")
SQL.ADD(" Select DISTINCT TipoOp ")
SQL.ADD(" from "~DBO~".OPERACIONES O ")
SQL.ADD("where O.FechaOp  <= '"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and (O.FechaVto>'"~IdxDate(Dialogo.Fecha1)~"' ")
*- Que Vencen en ese mes aunque no esten vigentes
SQL.ADD(" or (Year(O.FechaVto)="~Fstr(Anio(Dialogo.Fecha1),0,0)~" ")
SQL.ADD(" and Month(O.FechaVto)="~Fstr(Mes(Dialogo.Fecha1),0,0)~") ")
SQL.ADD(" ) ")
SQL.ADD(" and  O.TipoOp In ('MMPCCV') ")
if NoVacio(Dialogo.Especie1)
   SQL.ADD(" and O.Especie = '"~Dialogo.Especie1~"' ")
endif
if NoVacio(Dialogo.Lista2)
   SQL.ADD(" and TipoOp In ("~Pipe2Comillas(Dialogo.Lista2)~") ")
ELSE
   SQL.ADD(" AND TipoOp IN ("~Pipe2Comillas(Val(LS:2))~") ")
EndIf
SQL.ADD(" AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(VH:10))~") ")
If NoVacio(Dialogo.NrOperacion1)
   SQL.ADD("  and O.NrOperacion='"~Dialogo.NrOperacion1~"' ")
EndIf
SQL.ADD("        AND O.TotalIntereses > 0 ")

SQL.ADD(" UNION ")

SQL.ADD(" Select DISTINCT O.TipoOp ")
SQL.ADD(" From   "~DBO~".CUOTAS C, "~DBO~".OPERACIONES O ")
SQL.ADD(" Where  O.NrOperacion = C.NrOperacion ")
If NoVacio(Dialogo.NrOperacion1)
  SQL.ADD(" AND O.NrOperacion='"~Dialogo.NrOperacion1~"' ")
EndIf
SQL.ADD(" AND O.Vehiculo1 IN ("~Pipe2Comillas(Val(VH:10))~") ")
if NoVacio(Dialogo.Especie1)
   SQL.ADD(" and O.Especie = '"~Dialogo.Especie1~"' ")
endif
if NoVacio(Dialogo.Lista2)
   SQL.ADD(" and O.TipoOp In ("~Pipe2Comillas(Dialogo.Lista2)~") ")
ELSE
   SQL.ADD(" AND O.TipoOp IN ("~Pipe2Comillas(Val(LS:2))~") ")
EndIf
SQL.ADD("        AND O.TipoOp IN ('MMPCCM') ")
SQL.ADD(" and O.FechaOp<='"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and ((O.FechaVto>'"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and C.Fecha1<='"~IdxDate(Dialogo.Fecha1)~"' ")
SQL.ADD(" and C.Fecha2>'"~IdxDate(Dialogo.Fecha1)~"') ")
*- Que Vencen en ese mes aunque no esten vigentes
SQL.ADD(" or (Year(C.Fecha2)="~Fstr(Anio(Dialogo.Fecha1),0,0)~" ")
SQL.ADD(" and Month(C.Fecha2)="~Fstr(Mes(Dialogo.Fecha1),0,0)~") ")
SQL.ADD(" ) ")
SQL.ADD("        AND C.Cantidad3 > 0 ")
Recorrer SQL
    ACT(LS:9, Val(LS:9)~fbn('TipoOp')~'|')
Proximo