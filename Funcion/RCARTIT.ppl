SetFilaActual(1)
ACT(a:1,'Operaciones Financieras CONFIDENCIAL')
SetFont(a:1,2)
SetFilaActual(FAC+3)

ACT(f:2,'Fecha : '~Var('FECHASYS'))
SetFont(f:2..f:2,1)

ACT(a:FAC, "Reporte de Cartera")
SetFont(a:fac..a:fac,3)

SetFilaActual(FAC+2)

if NoVacio(Dialogo.Vehiculo2)
  ACT(a:FAC, 'Grupo :')
  ACT(b:FAC, Pipe2Comillas(Dialogo.Vehiculo2))
else
  if NoVacio(Dialogo.Lista1)
    ACT(a:FAC, 'Vehiculo/s :') 
    ACT(b:FAC, Dialogo.Lista1) 
  else 
    ACT(a:FAC, 'Vehiculos :')
    ACT(b:FAC, 'Todos')
  endif
endif

ACT(a:FAC+1, 'Especie :')
if NoVacio(Dialogo.Especie1)
  ACT(b:FAC+1, Dialogo.Especie1~'  '~BuscarCampo('Especies','Nombre','Codigo',Dialogo.Especie1))
else
  ACT(b:FAC+1, 'Todas')
endif

*ACT(a:FAC+2, 'Book :')
*if NoVacio(Val(r:3))
*  ACT(b:FAC+2, Val(r:3))
*else
*  ACT(b:FAC+2, 'Todos')
*endif
ACT(a:FAC+2, 'Tipos Book :')
If NoVacio(Dialogo.Lista2)
  ACT(b:FAC+2, Dialogo.Lista2)
else
  ACT(b:FAC+2, 'Todos')
endif
ACT(a:FAC+3, 'Books:')
if NoVacio(Dialogo.Lista3)
  ACT(b:FAC+3, Dialogo.Lista3)
else
  ACT(b:FAC+3, 'Todos')
endif

ACT(a:FAC+4, 'Consolidado :')
ACT(b:FAC+4, iif(Dialogo.Radio2=0,'SI','NO'))

ACT(a:FAC+5, 'A Nivel :')
ACT(b:FAC+5, IIF(Dialogo.Radio3=0,'Familias',IIF(Dialogo.Radio3=1,'Intermedio','Detalle')))

ACT(a:FAC+6, 'Expresado en :')
ACT(b:FAC+6, IIF(Dialogo.Radio1=0,'Pesos','Dolares'))

ACT(a:FAC+7, 'Fecha :')
ACT(b:FAC+7, Dialogo.Fecha1)

ACT(a:FAC+8, 'T.I.R. :')
if Dialogo.Check1=0
  ACT(b:FAC+8, 'Detallada')
else
  ACT(b:FAC+8, 'Resumida')
endif

SetFont(a:fac..N:FAC+8,2)

SetFilaActual(FAC+10)

ACT(b:FAC, '             E X P O S I C I O N')
ACT(b:FAC+1, 'Net Market')
ACT(b:FAC+2, 'Value')
ACT(c:FAC+1, '%')
ACT(e:FAC, '           R E N T A B I L I D A D')
ACT(d:FAC+1, 'P&L')
ACT(d:FAC+2, 'Realizado')
ACT(e:FAC+1, 'P&L No')
ACT(e:FAC+2, 'Realizado')
ACT(f:FAC+1, 'P&L')
ACT(f:FAC+2, 'Total')
ACT(g:FAC+1, 'TNA (%)')

ACT(i:FAC+2, 'Tipo de Op.')
ACT(j:FAC+2, 'Cant.Reg.')
ACT(k:FAC+2, 'Suma Posic.')

ACT(l:FAC+2, 'Linea N1')
ACT(m:FAC+2, 'Linea N2')
ACT(n:FAC+2, 'Linea N3')
ACT(o:FAC+2, 'Linea N4')
ACT(p:FAC+2, 'Especies TIR')
ACT(q:FAC+2, 'Saldo')
ACT(r:FAC+2, 'Grupo')
ACT(s:FAC+2, 'Calc.TIR')
ACT(t:FAC+2, 'A Nivel')
ACT(u:FAC+2, 'Eli.x Nivel')
ACT(v:FAC+2, 'Nivel')
ACT(w:FAC+2, 'Cont.cero')
ACT(x:FAC+2, 'Cant.<> 0')
ACT(y:FAC+2, 'Eli.x Cero')
ACT(z:FAC+2, 'Ult.Nivel')
ACT(ea:FAC+2, 'Rec.N1')
ACT(eb:FAC+2, 'Rec.N2')
ACT(ec:FAC+2, 'Rec.N3')
ACT(ed:FAC+2, 'Rec.N4')
ACT(ee:FAC+2, 'Eliminado')

SetFont(a:FAC..z:FAC+2,2)
SetFont(ea:FAC..ee:FAC+2,2)
AlinearCeldas(b:FAC+1..g:FAC+2,'C')
AgregarMarco(a:FAC..a:FAC+2,1,2,Exe('COLOR'))
AgregarMarco(b:FAC..c:FAC,1,0,Exe('COLOR'))
AgregarMarco(d:FAC..g:FAC,1,0,Exe('COLOR'))
AgregarMarco(b:FAC+1..g:FAC+2,1,2,Exe('COLOR'))

SetFilaActual(FAC+2)
ACN(w:1,FAC)
